---
title: Trainable Smooth-Rotation Transforms with Learned Channel Scales for LLM Quantization
title_zh: 可训练的平滑旋转变换与学习通道缩放用于LLM量化
authors: "Patrik Czakó, Gábor Kertész, Sándor Szénási"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.09927v1"
tags: ["query:moe-gk-quant"]
score: 9.0
evidence: 通过可训练平滑旋转变换实现LLM权重量化
tldr: "大语言模型后训练量化中，激活量化因异常值通道导致误差大。本文发现缩放等效变换存在过度迁移问题，提出基于高分位数的鲁棒缩放策略替代最大值统计，并联合梯度优化的通道缩放。在LLaMA-3.2-1B的W4A4量化上，单层量化误差降低11.1-18.5%，全层误差降低19.9%。该方法在保持等效变换框架下实现了稳定提升。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.09927v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 855, \"height\": 484, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09927v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 858, \"height\": 489, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09927v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 854, \"height\": 427, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09927v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 851, \"height\": 490, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09927v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 865, \"height\": 431, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09927v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 866, \"height\": 423, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09927v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 887, \"height\": 436, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.09927v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 549, \"height\": 262, \"label\": \"Table\"}]"
motivation: 激活量化受异常值通道影响大，现有缩放迁移策略因过度迁移加剧误差，需更鲁棒的迁移控制。
method: 采用高分位数替代最大值作为激活统计量，并联合梯度优化通道缩放因子，实现鲁棒的SmoothRot变换。
result: "在LLaMA-3.2-1B的W4A4量化中，单层误差降低11.1-18.5%，全层误差从97.51降至78.08（19.9%）。"
conclusion: 鲁棒迁移控制与轻量缩放学习可稳定提升量化性能，且不破坏等效变换框架。
---

## 摘要
后训练量化（PTQ）是降低大语言模型（LLM）服务成本最实用的方法之一，但激活量化仍然困难，因为异常值主导的通道会导致大量量化误差。本文研究了这种性能下降是否部分由基于缩放等效变换中的过度迁移引起。我们为SmoothRot风格变换引入了一种分位数稳健的缩放策略，用高分位数替代基于最大值的激活统计量，并结合基于约束梯度的通道缩放优化。在LLaMA-3.2-1B的W4A4量化下，仅分位数策略搜索相比SmoothRot基线将选定层误差降低了11.1%，联合(alpha, q)搜索降低了12%，训练达到18.5%。将最佳选定层策略重放至所有解码器块的下投影层，使相应的全层平均误差从97.51降至78.08（19.9%）。结果表明，稳健的迁移控制和轻量级缩放学习在保持等效变换框架的同时，相比基于最大值的固定策略提供了持续增益。

## Abstract
Post-training quantization (PTQ) is one of the most practical ways to reduce the serving cost of Large Language Models (LLMs), but activation quantization remains difficult because outlier-dominated channels lead to large quantization errors. This paper investigates whether part of this degradation is caused by over-migration in scaling-based equivalent transformations. We introduce a quantile-robust scaling policy for SmoothRot-style transforms by replacing max-based activation statistics with high quantiles, and we complement it with constrained gradient-based optimization of channel scales. On LLaMA-3.2-1B under W4A4 quantization, quantile-only policy search improves selected-layer error by 11.1% over the SmoothRot baseline, joint (alpha, q) search improves it by 12%, and training reaches 18.5%. Replaying the best selected-layer policy on all decoder-block down-projection layers reduces the corresponding full-layer mean error from 97.51 to 78.08 (19.9%). The results show that robust migration control and lightweight scale learning provide consistent gains over max-based fixed policies while preserving the equivalent-transform framework.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：大语言模型（LLM）的后训练量化（PTQ）在实际部署中至关重要，但激活量化因存在异常值主导的通道而面临严重误差。现有方法（如 SmoothQuant、SmoothRot）通过通道缩放等效变换来缓解，但采用最大值（max）统计量估计尺度，可能导致**过度迁移**（over-migration）——将量化困难从激活端过度转移到权重端，反而恶化整体量化误差。
- **核心问题**：本文旨在探究这种性能退化是否由缩放策略中的过度迁移引起，并提出更鲁棒的迁移控制方法。
- **整体含义**：通过替换 max 统计量为高分位数、并结合梯度优化的轻量级训练，可以在不破坏等效变换框架的前提下显著提升低比特（W4A4）量化精度，验证了“稳健迁移控制”是关键因素。

## 2. 方法论：核心思想、关键技术细节

### 2.1 核心思想
- 采用 SmoothRot 风格的参数化变换：先进行通道缩放（平滑），再进行 Hadamard 旋转。
- 缩放因子计算从 max 统计量改为基于分位数（quantile），减少对罕见尖峰异常值的敏感度。
- 引入两阶段优化：无训练阶段搜索缩放超参数（α, q）；训练阶段使用直通估计器（STE）梯度学习通道缩放向量 s。

### 2.2 关键技术细节
- **变换矩阵**：\(A = (H\Lambda)^{-1}\)，其中 \(H\) 是 Hadamard 正交矩阵，\(\Lambda = \text{diag}(s)\) 为缩放对角线矩阵。
- **缩放因子公式**：
  \[
  s_j(\alpha, q) = \frac{[Q_q(|X_{:,j}|)]^{\alpha}}{[\max_k |W_{j,k}|]^{1-\alpha}}
  \]
  - \(Q_q\)：经验分位数；\(q \to 1\) 时退化为 max。
- **无训练搜索**：扫描 α（0.25~0.75，步长 0.025）和 q（0.95~1.0，log 间隔 16 个点），以及联合 (α,q) 网格（top-5 交叉）。
- **训练优化**：
  - 优化目标：归一化线性输出误差 \(\tilde{E}\)（公式 6）。
  - 使用 AdamW、余弦学习率衰减、800 步训练、5 步 warmup、80 步 early stopping。
  - 学习率扫描范围 \([5\times10^{-3}, 2\times10^{-2}]\)。

### 2.3 算法流程（文字说明）
1. 对全模型下投影层进行基线 SmoothRot（α=0.5, q=1.0）评估，按线性输出误差排序，选出 3 个关键层 + 2 个正常层作为选定层集合 \(S_{\text{sel}}\)。
2. 在选定层上执行无训练族：N1（α 扫描，固定 q=1）、N2（q 扫描，固定 α=0.5）、N3（联合 (α,q) 网格）。
3. 执行训练族：T1（从旋转初始化 s=1 开始训练学习率扫描）、T2（用 N3 最优 warmstart 初始化 + T1 最佳学习率训练）、T3（将 T2 最佳策略重放到全部解码器块下投影层）。

## 3. 实验设计

- **数据集**：Wikitext-2，取 256 个样本，每个 128 token，80/20 分割为训练/测试集。
- **模型与量化设置**：LLaMA-3.2-1B，W4A4（4-bit 权重和激活）。
- **基准方法**：
  - QuaRot：旋转仅（s=1，无缩放）。
  - SmoothRot：原始基于 max 的缩放（α=0.5, q=1.0）。
- **对比方案**：无训练（N1/N2/N3）与训练（T1/T2）多组，以及全层重放（T3）。
- **评估指标**：线性输出误差 \(E = \|Y - \hat{Y}\|_F\) 及其归一化版本。

## 4. 资源与算力

- 文中**未明确说明**使用的 GPU 型号、数量及训练总时长。
- 仅提及训练步数 T=800，warmup=5，early stopping 80 步；模型为 1B 参数规模，采用单个数据集以加速实验。
- 由于未提供详细算力信息，无法评估训练开销的实际规模。

## 5. 实验数量与充分性

- **实验组数**：
  - N1：21 个 α 值扫描。
  - N2：16 个分位数值扫描。
  - N3：由 top-5 决策构成 25 个网格点（5×5）。
  - T1：5 个学习率扫描。
  - T2：5 个 warmstart 策略 × 2 个学习率 = 10 个子实验。
  - T3：最佳策略全层重放。
- **充分性评价**：
  - **优点**：系统性强，分离了无训练和训练贡献，验证了量化策略的可迁移性（选定层→全层）；消融清晰（α 单独 vs. q 单独 vs. 联合）。
  - **不足**：
    - 仅使用单模型（LLaMA-3.2-1B）和单数据集（Wikitext-2），存在过拟合风险。
    - 未在更大规模模型（7B/13B）、其他架构（GLU 激活）或下游任务（perplexity、对话基准）上验证。
    - 优化目标为线性输出误差，而非最终任务损失，可能无法完全对齐实际生成质量。
    - 未报告多次重复实验的统计显著性（如标准差），仅展示单次最优值。

## 6. 主要结论与发现

1. **分位数策略贡献最大**：在无训练模式下，仅调整 q（N2）使选定层误差降低 11.1%，而仅调 α（N1）无改善；联合搜索（N3）仅额外提升 0.9%（合计 12.0%），表明主要增益来自鲁棒缩放。
2. **训练进一步显著改进**：T1（旋转初始化+训练）最佳学习率 0.02 时误差降低 18.45%，超越所有无训练方法。
3. **T2 未超越 T1**：N3 暖启动未能带来额外收益，但两种训练最终学习到的通道间缩放模式高度相关（Pearson 0.98），仅整体幅度不同。
4. **全层重放效果一致**：T3 将全层平均误差从 97.51 降至 78.08（19.93%），关键层改善 17.66%，正常层改善 21.12%，优于老 SmoothRot 在正常层上的表现。
5. **机制洞察**：max 统计量在存在罕见尖峰时高估激活尺度导致过度迁移，分位数替代可缓解，训练进一步调谐通道相对模式。

## 7. 优点

- **方法简洁有效**：仅替换 max 为分位数 + 轻量梯度训练，无需修改网络结构或全模型微调，保持 PTQ 高效性。
- **实验设计分层清晰**：无训练/训练分离、选定层/全层验证，能够明确各环节贡献。
- **可迁移性好**：选定层策略直接重放到全部下投影层仍获得一致增益，说明策略具有泛化性。
- **发现有意义**：揭示了现有方法中过度迁移的具体故障模式，并提供了实用解决方案。
- **指标公平**：所有基线在同环境下重跑，排除了实现差异。

## 8. 不足与局限

- **模型/数据集覆盖过窄**：仅报告 LLaMA-3.2-1B + Wikitext-2 结果，缺乏在更大模型（如 7B/13B）、其他任务（Machine Translation、Code Generation）或不同量化配置（W8A8、W4A8）上的验证。
- **评估指标间接**：使用线性输出误差而非最终 perplexity 或下游任务准确率，可能无法完全反映对生成质量的真实影响。
- **训练开销未分析**：梯度优化需要前向/反向传播，训练时间/内存成本未量化，且未与端到端量化部署开销（如旋转矩阵存储、缩放因子存储）对比。
- **优化目标未覆盖所有层**：仅对下投影层优化，其他层（如 QKV 投影、FFN 升维层）的异常值问题可能未被处理。
- **统计显著性不足**：未报告多次重复实验的误差或置信区间，无法判断改进是否稳定。
- **局限性认知不足**：作者已提及这些局限，但未讨论如何扩展（如不同 token 长度、不同量化策略需要适应）。

（完）
