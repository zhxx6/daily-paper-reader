---
title: Value-and-Structure Alignment for Routing-Consistent Quantization of Mixture-of-Experts Models
title_zh: 面向路由一致的混合专家模型量化的值对齐与结构对齐
authors: "Hancheol Park, Geonho Lee, Tairen Piao, Tae-Ho Kim"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.05688v1"
tags: ["query:moe-gk-quant"]
score: 9.0
evidence: 面向MoE模型的路由一致量化
tldr: 混合专家模型量化时路由不稳定，微小扰动改变专家选择导致性能下降。提出VSRAQ方法，结合值对齐与结构对齐两个目标，在量化前后保持专家选择行为。实验证明VSRAQ提升了专家选择一致性，优于传统重建基线。该方法无推理开销，可集成到现有量化框架中。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.05688v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 788, \"height\": 537, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.05688v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 473, \"height\": 390, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05688v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 648, \"height\": 284, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05688v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 648, \"height\": 284, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05688v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 664, \"height\": 283, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05688v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 708, \"height\": 314, \"label\": \"Table\"}]"
motivation: MoE模型量化时路由不稳定，微小扰动改变top-k专家选择，降低模型质量。
method: 提出VSRAQ，通过值对齐匹配路由分数，结构对齐保持专家排序和决策边界。
result: 在近期MoE基础模型上，VSRAQ提升专家选择一致性，优于仅重建和路由器感知基线。
conclusion: VSRAQ通过保持路由一致性减少量化退化，无推理开销，可集成现有量化框架。
---

## 摘要
混合专家（MoE）模型通过仅为每个词元激活部分专家来高效扩展基础模型，但其大量的专家参数仍使得量化对于实际部署至关重要。然而，与稠密模型不同，MoE模型对路由不稳定性非常敏感：量化引入的微小扰动可能改变top-k专家选择，从而改变计算路径并降低模型质量。我们提出了一种针对MoE的后训练量化目标——值结构与路由对齐量化（VSRAQ），该目标在量化前后保持预量化的专家选择行为。VSRAQ结合了两个互补的目标来共同保持专家选择行为：值对齐，匹配与路由相关的logits或分数；结构对齐，保持专家排序和top-k决策边界。通过维持路由一致性，VSRAQ减少了量化导致的性能退化，且不引入任何推理开销，并可集成到现有量化框架中。在近期MoE基础模型上的实验表明，VSRAQ提升了专家选择的一致性，并且始终优于仅基于重构和路由感知的基线方法。

## Abstract
Mixture-of-Experts (MoE) models scale foundation models efficiently by activating only a subset of experts for each token, but their large number of expert parameters still makes quantization essential for practical deployment. Unlike dense models, however, MoE models are sensitive to routing instability: small quantization-induced perturbations can change the top-$k$ expert selection, altering the computation path and degrading model quality. We propose Value-and-Structure Routing Alignment for Quantization (VSRAQ), a MoE-specific post-training quantization objective that preserves pre-quantization expert-selection behavior under quantization. VSRAQ combines two complementary objectives that jointly preserve expert-selection behavior: value alignment, which matches routing-relevant logits or scores, and structure alignment, which preserves expert ordering and top-$k$ decision boundaries. By maintaining routing consistency, VSRAQ reduces quantization-induced degradation without introducing any inference-time overhead and can be integrated into existing quantization frameworks. Experiments on recent MoE foundation models show that VSRAQ improves expert-selection consistency and consistently outperforms reconstruction-only and router-aware baselines.

---

## 论文详细总结（自动生成）

# 论文总结：Value-and-Structure Alignment for Routing-Consistent Quantization of Mixture-of-Experts Models (VSRAQ)

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：混合专家（MoE）模型通过稀疏激活（每token仅激活top-k专家）实现高效扩展，但大量专家参数仍需要量化以降低存储和带宽需求。然而，MoE对量化扰动高度敏感：微小的数值误差可能导致路由器改变 top-k 专家选择，从而改变计算路径，使后续生成错误累积，性能下降显著。这与稠密模型不同——稠密模型的计算图固定，仅引入连续误差。
- **整体含义**：现有PTQ方法主要关注数值精度（如输出重构损失），但MoE场景下路由稳定性更重要。需要一种专门保持专家选择一致性的量化目标，以减少量化引起的性能退化，且不能引入推理开销。

## 2. 论文提出的方法论
- **核心思想**：提出VSRAQ，一种后训练量化校准目标，通过同时保持**路由值（Value Alignment）** 和**路由结构（Structure Alignment）** 来保持量化前后top-k专家选择一致。
- **关键技术细节**：
  - **值对齐（Value Alignment）**：
    - **TopK-MSE损失**：直接匹配被选top-k专家的logit值。
    - **Sigmoid-Aware 分数保持**：针对使用sigmoid路由的模型（如DeepSeek-V3），对敏感区域（sigmoid函数斜率大的部分，即概率≈0.5处）赋予更高权重，而对饱和区域（接近0或1）降低权重，避免校准容量浪费。使用权重 \( w = (p(1-p))^\gamma + \epsilon \)。
    - 最终值对齐损失：\( L_{\text{value}} = (1-\alpha) L_{\text{TopK}} + \alpha L_{\text{sig}} \)。
  - **结构对齐（Structure Alignment）**：
    - **顺序保持损失（L_rank）**：在量化前后保持被选top-k专家之间的排序关系，使用softplus函数惩罚相邻专家logit差小于边界的反转。
    - **边界保持损失（L_margin）**：保持top-k边界（第k个专家与附近非选中专家的logit差）在量化前后一致。
    - 最终结构对齐损失：\( L_{\text{struct}} = \beta_{\text{rank}} L_{\text{rank}} + \beta_{\text{margin}} L_{\text{margin}} \)。
  - **总目标**：\( L = L_{\text{recon}} + \lambda_{\text{router}} (L_{\text{value}} + L_{\text{struct}}) \)，其中 \( L_{\text{recon}} \) 是原框架的块输出重构损失，如AutoRound。
- **算法流程说明**：VSRAQ作为一个额外的校准损失，插入到现有PTQ框架（如AutoRound）的优化循环中，在少量校准数据上联合优化量化参数（剪切范围、缩放因子、零点、舍入方式）。推理时无任何额外计算或修改。

## 3. 实验设计
- **数据集 / 场景**：
  - **校准集**：NVIDIA Reasoning 校准集，由OpenCodeReasoning、OpenScienceReasoning-2、OpenMathReasoning混合构成，每个模型随机采样512条，序列长度Solar 2048、Nemotron 1024。
  - **评估集**：
    - 困惑度（WikiText-2）
    - 生成式推理任务（提取答案）：MMLU-Pro、GPQA-Diamond、GSM-8K（贪婪解码，非采样）
    - 似然型多项选择任务：ARC-C、ARC-E、BoolQ、HellaSwag、MMLU、PIQA、TruthfulQA、WinoGrande（报告平均分）
  - **专家一致性度量**：使用Nemotron-Post-Training-Dataset-v2的616,759 token，计算每层Jaccard相似度。
- **Benchmark**：对比方法：
  - Unquantized（全精度基线）
  - AutoRound（原始重构损失基线）
  - TopK-MSE（用EAC-MoE风格的top-k logit MSE损失作为辅助损失，集成到AutoRound中）
  - VSRAQ（本文方法）
- **模型**：
  - Solar-Open-100B（纯Transformer MoE）
  - Nemotron-3-Nano-30B-A3B（混合Mamba-Transformer MoE）
- **量化设置**：W4A16（权重量化4-bit，激活16-bit）、NVFP4（权重+激活均4-bit浮点）。

## 4. 资源与算力
- **文中未明确说明**使用的GPU型号、数量以及训练/校准时长。仅提到“不引入推理开销”，推理时资源与AutoRound同精度模型一致。未提及校准过程的硬件和时间细节。

## 5. 实验数量与充分性
- **实验数量**：
  - 主实验：3张表格（Table 2 Solar W4A16, Table 3 Solar NVFP4, Table 4 Nemotron NVFP4）覆盖两个模型、两种量化设置，对比三大评估维度。
  - 消融实验：Table 5（WikiText-2 PPL逐步添加组件：AutoRound → +TopK-MSE → +Sigmoid → +Structure）。
  - 路由一致性分析：Figure 1（W4A16下各层Jaccard相似度对比）。
- **充分性与公平性**：
  - 对比基线合理（使用最新方法TopK-MSE和AutoRound）。
  - 多种评估方式（困惑度、生成式、似然式）覆盖不同敏感性，且生成式任务使用贪婪解码排除采样随机性。
  - 消融实验验证了每个组件的贡献。
  - 在非Transformer架构（Nemotron混合Mamba）上验证了泛化性。
  - 无隐藏超参数调整优于基线的情况；超参数除λrouter外统一。
  - 不足之处：仅测试4-bit，未涉及更低位（2-bit、3-bit）；仅在两个模型上实验，扩展性有限；校准集规模较小（512条样本）。

## 6. 论文的主要结论与发现
- VSRAQ在困惑度、生成式推理任务和多项选择任务上一致优于AutoRound和TopK-MSE基线。
- 在生成式任务（推理长句子）上提升尤为明显，因为路由一致性减少了长生成中的累积误差。
- 专家选择一致性（Jaccard相似度）提升最高达11.29%（相对改善），证明VSRAQ确实保持了路由行为。
- 各组件（sigmoid感知、结构对齐）均贡献正面，但并非严格单调递增，整体效果优于只用TopK-MSE。
- 无推理开销，即插即用。

## 7. 优点
- **针对性**：专门针对MoE路由不稳定性设计，同时考虑logit值和排序、边界结构，更全面。
- **即插即用**：可作为额外损失嵌入任何基于重构的PTQ框架，无需修改推理图。
- **低开销**：仅在校准阶段增加计算，推理时无额外成本。
- **可解释性**：每个组件有明确动机（sigmoid敏感区域、排序保持、边界保持）。
- **实验设计稳健**：多种评估、多种模型、多种量化方案，公平对比。

## 8. 不足与局限
- **仅测试4-bit**：论文承认更低位（2/3-bit）可能受益更大，但未做实验验证，局限在中等噪声程度。
- **模型覆盖有限**：仅测试了两个MoE模型（Solar-Open-100B和Nemotron-3-Nano-30B），其他架构（如DeepSeek、GLM）未涵盖，泛化性存疑。
- **校准数据来源**：使用了特定的NVIDIA推理校准集，可能在其他分布上表现不同；校准集大小只有512条，较大模型可能不够。
- **依赖AutoRound**：实验基于该框架，与其他PTQ方法（如GPTQ、SmoothQuant）的兼容性未测试，集成时需要调整超参数。
- **超参数敏感性**：λrouter在两个模型上不同（0.01 vs 0.1），说明需要手动调节，缺乏自适应机制。
- **消融实验非严格单调**：在Nemotron上“+Sigmoid”后PPL未提升，结构对齐贡献也不均，提示组件间可能存在冲突或最优组合需搜索。
- **社会影响**：量化可能引入行为改变（如偏见、幻觉），论文仅简短提及未深入安全评估。

---

（完）
