---
title: "Bits and Memories: Measuring Verbatim Extraction Across LLM Quantization"
title_zh: 位与记忆：测量大语言模型量化中的逐字提取
authors: Akshay Sasi
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.25451v1"
tags: ["query:moe-gk-quant"]
score: 8.0
evidence: 直接研究权重量化对LLM记忆的影响
tldr: 语言模型部署前通常被量化，但现有研究多使用成员推理评估隐私风险，忽略了训练数据逐字提取的实际威胁。本文直接测量Pythia模型在不同量化精度下对已记忆序列的逐字提取能力，发现量化选择性遗忘：逐字记忆下降快于模型能力，但不足以作为隐私防御。在最大模型上4-bit量化仍复制大部分记忆序列，且幸存比例随模型增大而增长。结论：压缩不能移除记忆数据，应关注提取而非成员推理。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.25451v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1417, \"height\": 423, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25451v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1030, \"height\": 619, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25451v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 865, \"height\": 776, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.25451v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1112, \"height\": 715, \"label\": \"Table\"}]"
motivation: 现有研究用成员推理评估量化隐私风险，但逐字提取才是用户真正担忧的威胁，需直接测量。
method: 使用Pythia模型及其公开记忆序列，测试5种精度（全精度到4-bit）和3种模型大小，记录逐字提取率与困惑度。
result: 量化选择性遗忘：记忆下降快于能力，但4-bit下大模型仍复制多数记忆序列，且幸存比例随模型增大而增长。
conclusion: 压缩不能有效移除记忆数据，应使用提取而非成员推理评估量化隐私，量化不构成隐私防御。
---

## 摘要
语言模型在部署前几乎总是被量化，越来越多的研究在探讨量化是否也会降低其隐私风险。这些研究几乎完全通过成员推断来衡量隐私。我们认为，对于大多数人实际担心的风险——即模型逐字再现其训练数据——这是错误的衡量指标，因此我们直接测量了这一点。使用 Pythia 模型及其已知记忆的公开序列集，我们追踪了从全精度到四位五种精度水平下的逐字提取，跨越三种模型规模，同时测量每个节点的通用能力（困惑度）。我们发现了两点：量化是选择性的遗忘器——在我们尝试的每种精度和每个模型规模下，逐字记忆的下降速度都快于能力下降，并且这在两种无关的量化算法和两个评估语料库中均成立。但这种选择性不足以使量化成为一种隐私防御手段，这与早期成员推断结果的乐观解读相悖。在我们研究的最大模型中，四位量化仍然重现了大部分记忆序列，同时仅牺牲了百分之几的能力，并且量化中幸存下来的记忆数据比例随模型规模增大而增长。我们得出结论：压缩不应被视为移除记忆训练数据的方法，从业者应关注的是提取而非成员推断。所有代码、采样评估数据和每配置结果均已发布。

## Abstract
Language models are almost always quantized before they are deployed, and a growing line of work asks whether quantization also lowers their privacy risk. That work measures privacy almost entirely with membership inference. We think this is the wrong thing to measure for the risk that most people actually worry about, namely a model reproducing its training data word for word, and we measure that directly. Using the Pythia models and the public set of sequences each of them is known to have memorized, we track verbatim extraction across five precision levels, from full precision down to four bits, and across three model sizes, while measuring general capability (perplexity) at every point. We find two things. Quantization is a selective forgetter: verbatim memorization falls off faster than capability at every precision and every model size we tried, and this holds under two unrelated quantization algorithms and two evaluation corpora. But the selectivity is not enough to make quantization a privacy defense, which cuts against the optimistic reading of earlier membership-inference results. At the largest model we study, four-bit quantization still reproduces most of the memorized sequences while giving up only a few percent of capability, and the fraction of memorized data that survives quantization grows with model size. We conclude that compression should not be treated as a way to remove memorized training data, and that extraction, not membership inference, is the number practitioners should be watching. All code, sampled evaluation data, and per-configuration results are released.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：语言模型在部署前几乎都会被量化（如 8-bit、4-bit），这虽然降低了模型部署的硬件门槛，但也引发了隐私风险问题。现有研究主要使用**成员推断攻击（membership inference）** 来评估量化对隐私的影响，并报告量化能降低成员推断成功率，暗示量化有助于隐私保护。
- **核心问题**：论文指出，成员推断只能回答“某条数据是否在训练集中”，而**实际威胁**是用户能够**逐字提取（verbatim extraction）** 训练数据（如私人信息、版权内容）。目前尚无工作系统性地测量量化对逐字提取的影响，尤其是跨不同位宽和模型规模，并使用真实记忆数据。
- **整体含义**：论文试图直接测量量化如何改变模型对已知记忆序列的逐字提取能力，并与模型通用能力（困惑度）的变化进行对比，从而判断量化是否可以作为隐私防御手段。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：使用公开的、已验证的记忆序列作为**真实标签（ground truth）**，避免猜测模型记忆了什么。通过对比量化前后模型对这些序列的提取成功率以及困惑度的变化，量化“选择性遗忘”的程度。
- **关键技术细节**：
  - **模型与记忆数据**：使用 Pythia 模型家族（160M、410M、1B 参数），其训练语料库 Pile 和官方发布的记忆序列（Biderman et al. 2023b）均公开。序列长度为 64 token。
  - **提取评估**：将每个记忆序列拆分为前 32 token（提示）和后 32 token（目标），以贪心解码（温度=0）生成续写，若生成的 32 token 与目标完全一致，则计为一次成功提取。指标为**精确匹配率（exact-match rate）**，同时记录平均正确前缀长度和逐 token 准确率。
  - **能力评估**：在每个精度下，使用滑动窗口计算**困惑度（perplexity）**，分别在两个语料上进行：WikiText-2（域外）和 Pile 样本（域内），以避免分布偏移。
  - **量化方案**：
    - 主要精度阶梯：FP32、FP16、INT8（LLM.int8()）、NF4 和 FP4（来自 bitsandbytes 库）。
    - 独立验证方法：自行实现的**分组对称四舍五入量化（RTN）**，直接作用于线性层权重，与 bitsandbytes 的 normal-float 方案算法无关。
  - **选择性指标**：定义保留分数 \( m(q) = \frac{\text{extraction}(q)}{\text{extraction(ref)}} \)，\( c(q) = \frac{\text{perplexity(ref)}}{\text{perplexity}(q)} \)。选择性 \( s(q) = \frac{\log m(q)}{\log c(q)} \)。若 \( s > 1 \)，表示记忆比能力下降得更快。
- **流程概述**：对每个模型和精度组合，加载量化后的权重，依次运行提取评估和困惑度计算，所有序列固定随机子集，确保不同精度间数据一致。

## 3. 实验设计：数据集、benchmark、对比方法

- **数据集**：
  - **记忆评估**：Pile 语料中官方标记的 64-token 记忆序列，随机采样固定子集。
  - **能力评估**：WikiText-2（域外）和 Pile 文本样本（域内）。
- **Benchmark**：自身 full-precision（FP32 或 FP16）作为参考基准，没有与其他论文对比，而是内部不同精度、不同模型大小之间的对比。
- **对比方法**：
  - 主量化方法：bitsandbytes 库的 FP16、INT8、NF4、FP4。
  - 独立验证方法：自写 RTN（round-to-nearest）8-bit 和 4-bit。
- **实验规模**：3 个模型大小 × 5~6 个精度配置（FP32、FP16、INT8、NF4、FP4、RTN4），共约 15~18 组实验。每组实验在固定种子下运行，且对每个配置同时测量提取率和困惑度。

## 4. 资源与算力

论文**未明确说明**使用的 GPU 型号、数量和训练时长。仅提及 1B 模型在 FP32 下无法放入其 GPU，因此以 FP16 作为参考。未提供推理成本或时间细节。因此无法量化算力消耗。

## 5. 实验数量与充分性

- **实验数量**：核心结果展示在表 1（约 18 个配置），图 2 和图 3 呈现了所有点。每次实验均复用相同的记忆序列子集（固定种子），确保公平对比。
- **充分性分析**：
  - **覆盖性**：涵盖了从全精度到 4-bit 的常见量化位宽，覆盖了三种模型规模，使用了两种无关的量化算法（bitsandbytes 和 RTN）以及两种评估语料（WikiText 和 Pile）。这为结论提供了较好的普适性。
  - **控制变量**：每个精度下的提取率和困惑度是在完全相同的数据上测量的，避免了随机差异。选择性指标统一了度量标准。
  - **潜在不足**：
    - 仅使用 Pythia 一个模型家族，其他架构（如 LLaMA、Mistral）可能不同。
    - 最大模型规模仅 1B，远小于实际部署的百亿/千亿参数模型，规模趋势需要外推。
    - 仅覆盖权重后训练量化，未涉及激活量化、量化感知训练（QAT）等。
    - 仅使用贪心解码（严格提取），实际攻击可能使用采样或 beam search，概率性提取的成功率可能更高。
  - **整体评价**：实验设计在给定 scope 下是充分且公平的，但作者主动承认了局限性，并未过度泛化。

## 6. 论文的主要结论与发现

1. **量化是选择性的遗忘器**：在所有精度和模型大小下，逐字记忆的下降速度都快于通用能力的下降速度（选择性 s > 1），最高可达约 8（1B 模型，NF4）。该现象在两种量化算法和两个语料库中一致。
2. **量化不足以作为隐私防御**：尽管选择性遗忘存在，但记忆幸存比例仍然很高。例如 1B 模型在 4-bit NF4 下保留了约 72% 的记忆序列，而困惑度仅损失约 4%。幸存比例随模型规模增大而增长，暗示更大模型可能几乎不受影响。
3. **大型模型对量化的噪声吸收更好**：1B 模型在 4-bit 下几乎保留了全部能力，同时记忆保存率也高于小模型。说明模型越大，其记忆越不易被量化破坏。
4. **提取比成员推断更适合评估量化隐私风险**：成员推断结果显示量化降低隐私泄露，但提取实验显示量化并不能有效移除记忆，两者结论矛盾。实际威胁（逐字提取）应作为主要监测指标。

## 7. 优点：方法或实验设计亮点

- **使用真实记忆标签**：避免了依赖成员推断等间接指标，直接测量实际威胁。
- **引入能力对照**：同时测量困惑度，能够区分“模型忘记记忆”和“模型整体崩溃”，使选择性分析可信。
- **独立验证量化方法**：自行实现的 RTN 与 bitsandbytes 结果一致，增强了结果的鲁棒性。
- **定义统一的定量指标**：选择性 s 简单且有效，便于跨精度和规模比较。
- **代码和结果完全公开**：便于复现和后续研究。
- **清晰的危害解读**：正视了乐观解读量化隐私的误区，为从业者提供警示。

## 8. 不足与局限

- **模型家族单一**：仅测试 Pythia 系列，其架构和训练方式（如去重程度）可能影响记忆方式，结果不一定推广到其他模型（如 LLaMA、GPT-3 等）。
- **模型规模有限**：最大 1B 参数，无法直接代表部署级模型（7B+）。规模趋势需谨慎外推。
- **仅限后训练权重量化**：未考虑量化感知训练（QAT）或激活量化，这些技术可能改变记忆鲁棒性。
- **仅贪心解码**：真实攻击可能采用更强大的采样/搜索策略，提取成功率可能更高，论文可能低估了实际泄漏。
- **语言局限**：仅英语文本，其他语言或代码可能不同。
- **静态记忆集**：论文评估的是量化对已记忆内容的影响，不涉及量子化过程中新数据的记忆行为（如量化后的模型是否更容易记忆新样例）。
- **无理论分析**：对选择性遗忘的机制（为何记忆比能力更敏感）仅做了推测，缺少深入的解释性实验。

（完）
