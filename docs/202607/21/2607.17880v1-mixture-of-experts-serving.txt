Title: Mixture-of-Experts Serving

URL Source: https://arxiv.org/pdf/2607.17880v1

Published Time: Tue, 21 Jul 2026 01:51:23 GMT

Number of Pages: 24

Markdown Content:
# Mixture-of-Experts Serving 

Zhiyi Huang ∗ Qinpei Lou ∗ Tao Xiao †

July 2026 

Abstract 

Mixture-of-Experts (MoE) models route each token to only a few expert networks, distribut-ing the serving load across experts whose popularity shifts over time. A serving system must therefore dynamically decide how many GPUs to assign to each expert, trading off service la-tency against the cost of reconfiguring the assignment. We introduce a formal model of MoE Serving and initiate a principled study of online and offline algorithms for it. Our main result is a polynomial-time O(√log k)-competitive online algorithm, where k is the number of GPUs beyond one per expert. We complement it with a matching Ω( √log k) barrier for the online dual problem underlying our analysis. In the offline setting, we give a constant-factor approximation, show that MoE Serving is NP-hard, and rule out an FPTAS assuming ETH. 

> ∗

The University of Hong Kong. Email: zhiyi@cs.hku.hk, qinpeilou@connect.hku.hk 

> †

Huawei Taylor Lab. Email: xiaotao21@huawei.com arXiv:2607.17880v1 [cs.DS] 20 Jul 2026 1 Introduction 

Dense language models scale by making every token use more parameters, tying computation to capacity. Mixture-of-Experts (MoE) models [29, 32] break this tie by maintaining a collection of expert networks but routing each token to only one or a few of them, so capacity can grow while the per-token computation stays low [19, 22, 43]. The MoE architecture underlies prominent language models, including Gemini 3 Pro [26], Mixtral [31], DeepSeek-V3 [38], and GPT-OSS [1]. DeepSeek-V3, for instance, activates only 37 billion of its 671 billion parameters per token [38]. Sparsity keeps these models cheap to run, but it reshapes the inference problem. The serving load is no longer handled by one dense model, but routed through experts whose popularity shifts over time and across application domains. A serving system must therefore decide not only how many GPUs to provision, but also where to place that capacity among the experts as the routed workload changes. To serve DeepSeek-V3, for example, the decoding stage deploys its 256 routed experts across 320 GPUs — one expert per GPU, with the remaining GPUs holding redundant copies of the busiest experts. 1 The reported DeepSeek-V3 deployment recomputes the placement periodically (e.g., every ten minutes) from recently observed load using simple heuristics [38]. De-signing principled algorithms for efficient MoE serving is therefore an important research direction. 

1.1 MoE Serving Problem 

This paper introduces a theoretical model for MoE serving, and initiates the study of online and offline algorithms through competitive and approximation analysis. Consider m experts deployed on n > m GPUs. At each step t, the system observes the workload routed to each expert and decides how many GPUs to assign to it. There are two main costs to consider. The latency cost at each step is the time it takes for all experts to finish their workloads, determined by the ratio of workload to the number of GPUs for the most heavily loaded expert. The reconfiguration cost is proportional to the number of GPUs that change expert assignment from the previous step, e.g., the I/O cost of copying neural network weights, warming caches, and other overheads. We consider minimizing the sum of the two costs. 2

The model deliberately abstracts away low-level details while keeping the main algorithmic tension. Putting more GPUs on currently busy experts reduces latency, but frequent reallocation creates instability and reconfiguration overhead. 

1.2 Algorithms and Barriers 

We parameterize the running time and competitive/approximation ratios of the algorithms by the number of experts m and the number of GPUs n. It is often more natural to consider the number of extra GPUs k = n − m, beyond the initial assignment of one GPU per expert. 

Online MoE Serving Results 

• There is a polynomial-time O(√log k)-competitive online algorithm for MoE Serving. 

• There is no o(√log k)-competitive algorithm for the online dual problem arising in our primal-dual analysis.   

> 1We omit deployment details that are immaterial to our model, such as shared experts and other placement optimizations; the qualitative picture is accurate.
> 2A weighted sum would appear more general, but any weight only rescales the workloads, which already range over all positive reals. We therefore omit weights for notational simplicity.

1We first design a fractional online algorithm, building on the now-standard techniques of online primal-dual with Fenchel duality [5, 20, 21, 28] and Kullback-Leibler divergence regularization [12, 14, 15, 18]. Applied directly, these techniques give only a log k competitive ratio. Our analysis further exploits the smoothness of the latency cost function (doubling the assigned GPUs at best halves the cost) and sublinearity of its conjugate, to improve the competitive ratio to O(√log k). For rounding, we first examine existing techniques and find that the online rounding schemes for caching and paging [6, 10] adapt to MoE serving. These schemes, however, are computationally inefficient even in their original settings. We exploit the smoothness of the latency cost function once again to design a polynomial-time Lazy Threshold Rounding that preserves both the expected latency and reconfiguration costs up to a constant factor. Is the √log k factor inherent to MoE serving, or can it be improved? Although we cannot answer this question unconditionally, we show that √log k is a barrier for our online primal-dual analysis. It is both a blessing and a curse that an online primal-dual algorithm solves the primal and dual problems simultaneously, as the best achievable ratio is limited by the harder of the two. The dual is an online budget-allocation problem with an unknown stopping time. As requests keep arriving for a single expert, one must repeatedly decide how much dual mass to commit, with diminishing marginal payoffs. The offline benchmark knows the stopping time and can spread this mass evenly across the steps. The online algorithm does not, and is therefore forced to overcommit early. This tension drives a √log k-factor loss. 

Offline MoE Serving Results 

• There is a polynomial-time constant-factor approximation algorithm for MoE Serving. 

• There is no fully polynomial-time approximation scheme (FPTAS) for MoE Serving assuming the Exponential Time Hypothesis (ETH). The constant-factor approximation follows by solving the convex relaxation offline and rounding it, e.g., with the same scheme used by our online algorithm. For the static special case, where the configuration must remain fixed throughout, e.g., when reconfiguration is prohibitively expensive, we give a simple 2-approximation and an almost matching integrality gap of 2 − 1 

> m

.Finally, we prove that MoE Serving is computationally hard even beyond this relaxation barrier. By a reduction from the Densest k-Subgraph problem, we show that the static problem is already NP-hard. Together with the ETH-hardness of distinguishing dense and sparse k-subgraphs [40], the reduction also rules out an FPTAS. 

1.3 Further Related Work 

Online Algorithm Problems with Reconfiguration Costs. The reconfiguration cost alone has been studied, under various names, in the literature on online paging and caching [6, 23, 44], 

k-server [35, 39], and chasing convex bodies [13, 24, 42]. The sum of a reconfiguration cost and a state-dependent cost of specific forms has been studied in metrical task systems [8, 11] and online set cover with service costs [14]. The state-dependent cost in these two problems, in their usual fractional formulations, is linear in the state, 3 whereas our latency cost is nonlinear. The best competitive ratios for all the above problems are logarithmic or worse, except in special cases.      

> 3Metrical task systems are often defined with an arbitrary cost over a finite set of states, which is equivalently a linear cost over distributions on those states, i.e., over x≥0 with ∥x∥1= 1.

2Smoothed online convex optimization [3, 7, 17] and convex function chasing [4] study the sum of a reconfiguration cost and a general convex state-dependent cost, capturing the fractional relaxation of MoE serving as a special case. For arbitrary convex costs, the competitive ratio depends polyno-mially on the dimension [4, 17]. Improved algorithms and competitive ratios are only known under structural assumptions that MoE serving does not satisfy, including one-dimensional decisions [7], locally polyhedral costs [17], or strongly convex and smooth costs [4]. 

MoE Expert Imbalance and Placement. Go and Mahajan [25] optimized a static expert placement by solving an integer program. Li et al. [37] instead showed empirically that expert load is skewed and shifts over time, so dynamically reallocating to the predicted load helps. However, they did not consider the reallocation cost, such as the overhead for copying the experts’ weights mentioned in the DeepSeek-V3 report [38]. This paper initiates the principled study of the trade-off between service latency and reconfiguration overhead. 

Other Challenges in Language Model Serving. Serving language models raises a range of scheduling problems. The natural scheduling granularity is a token-generation iteration, and high GPU utilization relies on effectively batching the active requests [2, 48]. A further difficulty is that a request’s output length is unknown when it arrives [47]. Compounding this, the key-value (KV) cache usage grows with the output length and can become the dominant memory constraint [36]. Classical scheduling theory considered batching and unknown job lengths separately, well be-fore the language model era. The former has been studied for objectives such as makespan [45], completion time [16, 45], and flow time [27]. The latter was commonly known as non-clairvoyant scheduling [9, 33, 41]. A recent line of theoretical work studied scheduling under a growing KV cache usage, in com-bination with batching and unknown job lengths. Wang et al. [46] proved NP-hardness of the offline problem with heterogeneous prefill and decode lengths, and gave a constant-factor approxi-mation. Jaillet et al. [30] studied the online problem against a hindsight-optimal integer program and obtained a constant competitive ratio under further conditions. Kong et al. [34] exploited the spatio-temporal geometry of cache growth to sharpen the competitive ratio toward 3 in the high-concurrency regime. 

# 2 Preliminaries 

Notation. Let Z and R denote the sets of integers and real numbers, respectively. We use subscripts ≥ 0 and > 0 to indicate the sets of non-negative and positive numbers. For example, 

Z≥0 and Z>0 are the sets of non-negative and positive integers, respectively. For any positive integer m, let [ m] denote the set {1, 2, . . . , m }.

Dynamic Model. We consider the Dynamic MoE Serving problem, where we deploy an MoE LLM consisting of m experts onto n > m GPUs. The system processes requests over T discrete time steps. At each step t ∈ [T ], the algorithm receives a request represented as a workload vector 

rt = ( rt,i )i∈[m] ∈ Rm

> ≥0

, where rt,i represents the volume of user requests routed to expert i.Upon observing rt, the algorithm picks a deployment configuration ct = ( ct,i )i∈[m] ∈ Zm>0, where 

ct,i is the number of GPUs allocated to expert i. The constraint ct,i ≥ 1 ensures that every expert is available at all times. This effectively means that the first m GPUs are allocated evenly to the experts. Analytically, it is more convenient to track the allocation of the remaining GPUs. Let 3xt,i = ct,i − 1 ∈ Z≥0 denote the number of remaining GPUs assigned to expert i, subject to the budget constraint that P 

> i∈[m]

xt,i = k def 

= n − m.Given a configuration xt, the latency cost of the system at step t is determined by the time it takes for all experts to complete their workloads: max 

> i∈[m]

rt,i 

1 + xt,i 

.

Furthermore, transitioning from the previous configuration xt−1 to the new configuration xt

incurs a reconfiguration cost proportional to the L1 distance between the allocations: 

X

> i∈[m]

xt−1,i − xt,i ,

where x0 is an arbitrary initial configuration given as part of the instance. The objective of the Dynamic MoE Serving problem is to minimize the sum of the total latency cost and the total reconfiguration cost over the time horizon [ T ]: 

X 

> t∈[T]



max 

> i∈[m]

rt,i 

1 + xt,i 

+ X

> i∈[m]

xt−1,i − xt,i 



.

Static Model. The Static MoE Serving problem is the variant where reconfiguration is strictly prohibited, meaning xt = x for all t ∈ [T ]. Because the reconfiguration cost is zero, the problem simplifies to finding a single, fixed configuration x ∈ Zm 

> ≥0

subject to P 

> i

xi = k that minimizes the total latency cost: TX

> t=1

max 

> i∈[m]

rt,i 

1 + xi

.

Approximation and Competitive Ratios. Let OPT denote the minimum total cost achieved by the optimal schedule in hindsight. Let ALG denote the total cost incurred by the algorithm’s schedule. For any Γ ≥ 1, an offline algorithm yields a Γ-approximation if E ALG  ≤ Γ · OPT for all instances. We analyze offline approximation algorithms for both the static and dynamic models. For any Γ ≥ 1, an online algorithm is Γ-competitive if E ALG  ≤ Γ · OPT + C for all instances, where C is an additive term independent of the time horizon T (but can depend on the numbers of experts and GPUs). We consider online algorithms only in the dynamic model. 

# 3 Convex Programs 

This section derives the convex program relaxation of the Dynamic MoE Serving problem and its Fenchel dual. The proofs of the lemmas are standard, and therefore deferred to Section A. For each step t ∈ [T ], define a convex function ft to denote the latency cost of configuration xt,extended to non-negative real vectors: 

ft(xt) def 

=



max i∈[m]

rt,i 

1 + xt,i 

if xt,i ≥ 0 for all i ∈ [m]; +∞ otherwise. To linearize the reconfiguration cost, we let yt,i ≥ max {xt,i − xt−1,i , 0} denote the incremental cost of expert i from step t − 1 to step t. The budget k is constant at all times (i.e., P 

> i

xt,i =4P 

> i

xt−1,i = k). Hence, the incremental cost matches the decremental cost, and equals half the total reconfiguration cost. Putting together, we have the following convex program relaxation of Dynamic MoE Serving, and denote its objective value as Primal and the optimal value as Primal ⋆.minimize X 

> t∈[T]

ft(xt) + X 

> t∈[T]

X

> i∈[m]

yt,i (Primal )subject to X

> i∈[m]

xt,i ≤ k ∀t ∈ [T ]

yt,i ≥ xt,i − xt−1,i ∀i ∈ [m], ∀t ∈ [T ]

xt,i , y t,i ≥ 0 ∀i ∈ [m], ∀t ∈ [T ]We do not attempt to optimize the constants in this paper, and thus, omit the factor 2 difference between incremental and total reconfiguration costs for ease of notation. There is an additional technical relaxation. Although all algorithms we design maintain the equality-budget constraint P 

> i

xt,i = k at every time step, in the primal relaxation used for the dual analysis we relax this equality by P 

> i

xt,i ≤ k. This can only decrease the fractional optimum, so the relaxation remains a valid lower bound on the optimal integral cost. The inequality form is more convenient for our argument. 

Lemma 3.1. For any instance of Dynamic MoE Serving, we have Primal ⋆ ≤ OPT .

We use an offset Fenchel dual of the primal relaxation. For each t ∈ [T ], define the function ˆft : Rm 

> ≥0

→ R ∪ {−∞} with respect to a non-negative dual variable γt ≥ 0: ˆft(γt) def 

= inf 

> xt≥0

 ft(xt) + ⟨xt, γ t⟩ .

The standard convex conjugate of ft is f ⋆t (γt) = sup xt≥0

 ⟨xt, γ t⟩− ft(xt). The above convention is equivalent to ˆft(γt) = −f ⋆t (−γ), which keeps γt and ˆft(γt) non-negative. With this definition, we recover ft as the pointwise supremum of its affine minorants: 

ft(xt) = sup 

> γt≥0

  ˆft(γt) − ⟨ xt, γ t⟩ .

Remark 3.2 (Subgradient Convention) . We use ∂f t(xt) to denote the subdifferential of the finite latency-cost expression 

xt 7 → max 

> i∈[m]

rt,i 

1 + xt,i 

on its natural domain xt,i > −1. The normal cone from the constraint xt ≥ 0 is handled separately by the convex program. With this convention, ∂f t(xt) is the convex hull of the gradients of the active coordinates attaining the maximum. 

Lemma 3.3. For any t ∈ [T ], the following properties hold. 1. (Conjugate Pair) For any γt ∈ − ∂f t(xt), ft(xt) = ⟨γt, x t + 1⟩.2. (Smoothness of ft) For any θ ≥ 1 and xt, x ′ 

> t

∈ Rm 

> ≥0

satisfying xt,i + 1 ≤ θ(x′ 

> t,i

+ 1) for every 

i ∈ [m], we have ft(x′

> t

) ≤ θf t(xt).3. (Sublinearity of ˆft) For any θ ∈ (0 , 1] and γt ≥ 0, ˆft(θγ t) ≥ √θ ˆft(γt).

5The dual variables are αt ∈ R≥0 for the budget constraints, βt,i ∈ R≥0 for the incremental reconfiguration constraints, and γt,i ∈ R≥0 for the Fenchel representation of ft. We also use an auxiliary terminal variable βT +1 ∈ Rm 

> ≥0

to write the dynamic constraints uniformly. The resulting offset Fenchel dual program is: maximize X

> t

ˆft(γt) − X

> t

kα t (Dual )subject to γt,i + βt+1 ,i − βt,i ≤ αt ∀t ∈ [T ], ∀i ∈ [m]0 ≤ βt,i ≤ 1 ∀t ∈ [T + 1] , ∀i ∈ [m]

γt,i ≥ 0 ∀t ∈ [T ], ∀i ∈ [m]

αt ≥ 0 ∀t ∈ [T ]Let Dual denote the objective value of the above program and the optimal value as Dual ⋆.We relate the optimal primal and dual objective values as follows. 

Lemma 3.4. For any instance of Dynamic MoE Serving, we have Dual ⋆ ≤ Primal ⋆ + k.

Karush-Kuhn-Tucker Conditions. While we will not use the optimality conditions of the optimal primal and dual solutions directly, we include them below for easy comparison with the optimality conditions of the projection steps of our online algorithm. Let x⋆, y ⋆ and α⋆, β ⋆, γ ⋆ be the optimal primal and dual solutions. They satisfy not only the primal and dual constraints, but also the following optimality conditions, including stationarity and complementary slackness: 

−γ⋆t ∈ ∂f t(x⋆t ) ,β⋆t,i 

 y⋆t,i − x⋆t,i + x⋆t−1,i 

 = 0 ,y⋆t,i 

 1 − β⋆t,i 

 = 0 ,x⋆t,i 

 α⋆t + β⋆t,i − β⋆t+1 ,i − γ⋆t,i 

 = 0 ,α⋆t

  X 

> i∈[m]

x⋆t,i − k = 0 .

# 4 Dynamic Model 

This section considers the Dynamic MoE Serving problem, for which we give efficient online and offline algorithms. 

Theorem 4.1. The Dynamic MoE Serving problem: 1. There is a polynomial-time O(√log k)-competitive online algorithm; and 2. There is a polynomial-time O(1) -approximation algorithm. 

We devote the rest of this section to proving Theorem 4.1. Section 4.1 gives a regularization-based fractional algorithm and derives the stated competitive ratio of O(√log k) (Lemma 4.2). Section 4.2 then presents an online rounding scheme that preserves both the latency cost and reconfiguration cost of the fractional algorithm up to a constant factor (Lemma 4.4). Combining the two lemmas proves the online part of Theorem 4.1; for the offline approximation, we solve the equality-budget version of the convex relaxation, i.e., with P 

> i

xt,i = k for all t, and then apply Lemma 4.4. 64.1 Regularized Greedy Algorithm 

Let η = max {1, ln( k +1) } be the regularization parameter, chosen to optimize the competitive ratio from our analysis. The regularizer is the generalized Kullback-Leibler (KL) divergence: 

DKL (u∥v) def 

= X

> i∈[m]



ui ln ui

vi

− ui + vi



.

For the configuration vectors in dynamic MoE serving, we use ui = xt,i + 1 and vi = xt−1,i + 1. In our algorithm, we use P 

> i

xt,i = k as our budget constraint rather than P 

> i

xt,i ≤ k. For every step t ∈ [T ], the linear terms cancel out since the total auxiliary budget is constant, i.e., P 

> i∈[m]

xt,i = P 

> i∈[m]

xt−1,i = k. Thus, the divergence simplifies to the standard relative entropy. 

Regularized Greedy Algorithm 

At every step t ∈ [T ], select a feasible fractional configuration xt that minimizes the sum of the current latency cost and the divergence from the previous configuration: 

xt = arg min    

> x∈Rm
> ≥0:P
> i∈[m]xi=k



ft(x) + DKL (x + 1 ∥ xt−1 + 1)



.

Lemma 4.2. Regularized Greedy is O(√log k)-competitive. Proof. KKT Conditions of Regularized Greedy. The update step is a convex minimization problem over the simplex-like constraint set x ∈ Rm 

> ≥0

| P 

> i∈[m]

xi = k . The Lagrangian is: 

Lt(xt, α ′

> t

) = ft(xt) + X

> i∈[m]



(xt,i + 1) ln xt,i + 1 

xt−1,i + 1 − xt,i + xt−1,i 



+ α′

> t

 X

> i∈[m]

xt,i − k



,

where α′ 

> t

∈ R is the Lagrange multiplier for the budget constraint. The KKT conditions imply that at the optimum xt, there are α′ 

> t

∈ R and γ′ 

> t

∈ Rm 

> ≥0

such that: 

−γ′ 

> t

∈ ∂f t(xt) , (1) 

−γ′ 

> t,i

+ ln xt,i + 1 

xt−1,i + 1 + α′ 

> t

≥ 0 ∀i ∈ [m] , (2) where the latter holds with equality if xt,i > 0. 

Dual Construction and Feasibility. Comparing Equation (2) with the first dual constraint, 

γt,i + βt+1 ,i − βt,i ≤ αt, it is natural to consider αt = α′

> t

, γt,i = γ′

> t,i

, and βt+1 ,i − βt,i = − ln xt,i +1  

> xt−1,i +1

.However, this is infeasible in general, because the cumulative changes in β may be as large as ln( k + 1) when xt,i ranges from 0 to k over time, violating the bounds 0 ≤ βt,i ≤ 1. The standard fix in the regularized greedy framework (e.g., [14, 15]) is to scale the regularizer. Indeed, changing the update step to minimize ft(x) + 1ln( k+1) DKL (x + 1 ∥ xt−1 + 1) would make the above dual construction feasible, but result in an inferior competitive ratio of O(log( k + 1)). Instead, we directly scale the dual variables to restore dual feasibility, and exploit the smoothness of ft and the sublinearity of ˆft to bound the loss in the competitive ratio. Let: 

αt def 

= 1

η α′ 

> t

, βt,i def 

= − 1

η ln xt−1,i + 1 

k + 1 , γt def 

= 1

η γ′ 

> t

.

7Then, Equation (2) becomes identical to the first dual constraint: 

γt,i + βt+1 ,i − βt,i ≤ αt , (3) with equality if xt,i > 0. Meanwhile, 0 ≤ βt,i ≤ 1 follows from the definition and 0 ≤ xt,i ≤ k.Finally, we show that: 

αt ≥ 0 , (4) which satisfies the last constraint of original dual program. Since ∥xt∥1 = ∥xt−1∥1 = k following the algorithm, we have xt,i ≤ xt−1,i for some i. Combining with γ′ 

> t,i

≥ 0 and Equation (2), we get that α′ 

> t

and thus αt are non-negative. 

Comparing the Primal and Dual Objectives. Recall that: 

Primal = X 

> t∈[T]



ft(xt) + X

> i∈[m]

yt,i 



, Dual = X 

> t∈[T]

  ˆft(γt) − kα t

 ,

where yt,i = max(0 , x t,i − xt−1,i ). The regularized greedy update ensures that the reconfiguration cost is bounded by the latency cost. This standard proof is deferred to Section B.1. 

Lemma 4.3. For every step t ∈ [T ], P 

> i

yt,i ≤ ft(xt).

Now consider the dual objective. Since the algorithm guarantees P 

> i

xt,i = k and the KKT Equation (3), we rewrite the second term of the dual objective as: 

X 

> t∈[T]

kα t = X 

> t∈[T]

X

> i∈[m]

xt,i 

 γt,i + βt+1 ,i − βt,i 

 .

Further by γt = 1 

> η

γ′

> t

, the dual objective equals: 

X 

> t∈[T]



ˆft

 1

η γ′

> t



−

D

xt, 1

η γ′

> t

E

+ X 

> t∈[T]

⟨xt, β t − βt+1 ⟩ .

By the sublinearity of ˆft (Lemma 3.3) and γ′ 

> t

≥ 0, the first part can be bounded by: 1

√η

X 

> t∈[T]

 ˆft(γ′

> t

) − ⟨ xt, γ ′

> t

⟩



= 1

√η

X 

> t∈[T]

ft(xt) .

The second term is bounded by telescoping: 

X 

> t∈[T]

⟨xt, β t − βt+1 ⟩ = X 

> t∈[T]

⟨xt + 1, β t − βt+1 ⟩ + ⟨1, β T +1 − β1⟩

= 1

η

X 

> t∈[T]

DKL (xt + 1 ∥ xt−1 + 1) + ⟨1, β T +1 − β1⟩ .

The KL-divergence is non-negative; the rest is at least −m, as βT +1 ,i ≥ 0 and β1,i ≤ 1 for all i.Putting everything together, we conclude that the dual objective satisfies: 

Dual ≥ 1

√η

X 

> t∈[T]

ft(xt) − m ≥ 12√η Primal − m . 

Combining the above with Dual ≤ Dual ⋆, Dual ⋆ ≤ Primal ⋆+k (Lemma 3.4) and Primal ⋆ ≤

OPT (Lemma 3.1), we get that: 

Primal ≤ 2√η · (OPT + m + k) .

With η = max {1, ln( k + 1) }, the competitive ratio is O(√log k). 84.2 Online Rounding 

Regularized Greedy only provides a fractional configuration xt ∈ Rm 

> ≥0

at each step. To obtain a valid deployment strategy, we must convert this into a feasible integral configuration ¯ xt ∈ Zm

> ≥0

satisfying P 

> i∈[m]

¯xt,i = k, while ensuring that the total cost does not increase significantly. In fact, the online rounding algorithm for the paging and caching problem [6, 10] can be adapted to our setting. Their algorithms, with appropriate changes, can simultaneously guarantee that (1) ¯xt,i ∈ {⌊ xt,i ⌋, ⌈xt,i ⌉} and E[¯ xt,i ] = xt,i for all i, and (2) the expected reconfiguration cost is at worst double, i.e., E[∥¯xt − ¯xt−1∥1] ≤ 2∥xt − xt−1∥1. These conditions ensure that the objective of the rounded solution is at most twice that of the fractional solution. These online rounding algorithms, however, are not computationally efficient, even in their original paging and caching settings. Instead, we utilize the smoothness of ft to design a simpler polynomial-time online rounding procedure that loses at worst a constant factor compared to the fractional solution. 

Lazy Threshold Rounding 

At the beginning, sample thresholds θi ∈ [0 , 1) independently and uniformly for i ∈ [m]. For each time step t = 0 , 1, . . . , T :

• Let ℓt,i = max 0, ⌊xt,i − θi⌋ for i ∈ [m]. 

• If t = 0, let ¯ x0 = x0.

• Otherwise, let ¯ xt be a feasible solution of: 

ℓt,i ≤ ¯xt,i ≤ max {ℓt,i , ¯xt−1,i } ∀i ∈ [m], X

> i∈[m]

¯xt,i = k . 

The definition of ¯ xt for t ≥ 1 is feasible because the lower bounds sum to at most P 

> i

xt,i = k,and the upper bounds sum to at least P 

> i

¯xt−1,i = k.

Lemma 4.4. Lazy Threshold Rounding runs in polynomial-time, and ensures that at every step t:1. ft(¯ xt) ≤ 3ft(xt); and 2. E [∥¯xt − ¯xt−1∥1] ≤ ∥ xt − xt−1∥1.Proof. (Running Time) We can greedily start from the upper bounds, and repeatedly reduce a variable strictly greater than its lower bound by 1 until the total mass is k.

(Latency Cost) For every expert i, the rounding rule gives: ¯xt,i ≥ ℓt,i = max {0, ⌊xt,i − θi⌋} ≥ max {0, x t,i − 2} .

Hence, 3(1 + ¯ xt,i ) ≥ 1 + xt,i . By Lemma 3.3, we have ft(¯ xt) ≤ 3ft(xt). 

(Reconfiguration Cost) The algorithm maintains ¯ xs,i ≥ ℓs,i for every step s and expert i. Fix a coordinate i. Since ¯ xt−1,i ≥ ℓt−1,i , the rounding rule implies (¯ xt,i − ¯xt−1,i )+ ≤ (ℓt,i − ℓt−1,i )+ .

9If xt,i ≤ xt−1,i , then ( ℓt,i − ℓt−1,i )+ = 0. Otherwise, ( ℓt,i − ℓt−1,i )+ is at most the number of thresholds θi + j for j ∈ Z≥0 that are crossed by the interval ( xt−1,i , x t,i ]. Since θi is uniform over one full period [0 , 1), the expected number of crossed thresholds is at most the interval length: 

E [(¯ xt,i − ¯xt−1,i )+] ≤ (xt,i − xt−1,i )+ .

Summing over i proves the desired bound for incremental cost, which is exactly half the total reconfiguration cost given that the total allocation is fixed at k at all times. 

# 5 Hardness for Online Dual Problem 

This section proves that the competitive ratio in Theorem 4.1 is asymptotically tight for the dual problem. Recall the latency cost ft and its signed Fenchel conjugate ˆft:

ft(xt) = max 

> i

rt,i 

xt,i + 1 , ˆft(γt) = inf 

> xt≥0

 ft(xt) + ⟨xt, γ t⟩ .

Online Dual Problem. At each time step t ∈ [T ], after observing the workload vector rt ∈ Rm

> ≥0

,the algorithm chooses: 

γt ∈ Rm 

> ≥0

, αt ≥ 0 , βt+1 ∈ [0 , 1] m ,

where βt is already fixed from the previous step t − 1. The chosen variables must satisfy: 

γt,i + βt+1 ,i − βt,i ≤ αt ∀i ∈ [m] .

The goal of this problem is to maximize the accumulated dual value: 

Dual = X 

> t∈[T]

bft(γt) − X 

> t∈[T]

kα t .

The online primal-dual analysis in Theorem 4.1 maintains feasible solutions for both primal and dual, with objective values differing by at most an O(√log k) factor. Hence, the O(√log k)competitive ratio applies to the primal and dual problems alike. The theorem below shows that √log k is an inherent information-theoretic barrier for the online dual problem, even if we allow computationally unlimited algorithms. 

Theorem 5.1. There is no online algorithm for the online dual problem with competitive ratio better than 132 

√ln k.

It suffices to consider deterministic algorithms. The dual problem has a convex feasible region and a concave maximization objective, so randomization cannot improve the expected value beyond the best deterministic action for a fixed history. Against deterministic algorithms, without loss of generality we can consider an adaptive adversary, who at each time observes the dual variables already chosen by the online algorithm and decides the workload vector. We prove the hardness with m = k + 1 experts, assuming k ≥ 1024, since excluding finitely many small k does not change the asymptotic bound. 10 Single Phase Instance. We start by describing a gadget that produces a √log k multiplicative gap between the online and offline dual objectives. Repeating this gadget for sufficiently many times yields the lower bound even with the additive term in the definition of competitive ratio. Fix an expert q. The instance repeatedly requests only q for τ steps where τ will be determined adaptively according to the algorithm’s decisions. Formally, for every time step t in this instance: 

rt,i =

(

1 if i = q;0 otherwise. Since only expert q has non-zero workload, the latency cost function and signed Fenchel conju-gate function can be written as: 

ft(xt) = 11 + xt,q 

, ˆft(γt) = 

(2√γt,q − γt,q , γt,q ∈ [0 , 1] ,

1, γt,q ≥ 1 .

In either case, we have ˆft(γt) ≤ 2√γt,q , which we will use in the arguments below. Define the objective value up to time t as: 

Dt def 

= X

> t′∈[t]

bft′ (γt′ ) − X

> t′∈[t]

kα t′

Let the stopping time τ be the first step τ ≥ 4 satisfying: 

Dτ ≤ 16 

r τ

ln k .

Lemma 5.2. For any online algorithm, the stopping time τ is well-defined. Proof. We prove a stronger claim that τ is at most: 

K =

 k2

64 



.

Assume for contradiction that for every τ ∈ [4 , K ], 

Dτ > 16 

r τ

ln k .

We next examine P 

> t∈[K]

γt,q and derive contradictory lower and upper bounds for it. 

Lower Bound. The online algorithm needs to stay competitive for every τ ∈ [4 , K ], and hence is forced to overcommit early when τ is small. We next argue that this overcommitment implies a lower bound on P 

> t∈[K]

γt,q . Since bft(γt) ≤ 2√γt,q and αt ≥ 0, we have Dτ ≤ 2 P 

> t∈[τ]

√γt,q for 

τ ∈ [4 , K ]. Therefore: X 

> t∈[τ]

√γt,q > 8

r τ

ln k . (5) By Hardy’s inequality: 

X

> t∈[K]

γt,q ≥ 14

X

> t∈[K]

P

> t′∈[t]

√γt′,q 

t

!2

.

11 Combining with Equation (5), we obtain: 

X

> t∈[K]

γt,q ≥ 16 ln k

> K

X

> t=4

1

t .

Using k ≥ 1024 and K = ⌊k2/64 ⌋:

> K

X

> t=4

1

t ≥

Z K+1 4

dx x = ln( K + 1) − ln 4 ≥ ln k . 

Thus, we conclude that: X

> t∈[K]

γt,q ≥ 16 .

Upper Bound. We next show that P 

> t∈[K]

γt,q cannot be too large, otherwise DK will be too small because of the −k P 

> t∈[K]

αt term. Since bft(γt) ≤ 2√γt,q , DK ≤ 2 P

> t∈[K]

√γt,q − k P 

> t∈[K]

αt.By Cauchy–Schwarz, we have: 

DK ≤ 2

s

K X

> t∈[K]

γt,q − k X

> t∈[K]

αt .

Summing the online constraints on the requested coordinate gives 

X

> t∈[K]

γt,q ≤ 1 + X

> t∈[K]

αt ,

because the total decrease of β is at most 1. This implies: 

DK ≤ 2

s

K X

> t∈[K]

γt,q − k

 X

> t∈[K]

γt,q − 1

 .

By DK > 0 and the fact that √K ≤ k 

> 8

, we have 4 P 

> t∈[K]

γt,q −

qP 

> t∈[K]

γt,q − 4 < 0. Therefore: 

X

> t∈[K]

γt,q < 43 .

Comparing the lower and upper bounds shown above, we have a contradiction. 

Lemma 5.3. Consider a single phase instance. Suppose β1,q = 1 in the beginning of the instance. There is an offline strategy simultaneously satisfying: 1. The dual objective value is at least √τ ; and 2. Every expert j̸ = q has its β-value increased by 1/k (capped at 1) in the end. 

12 Proof. Assign the unit β-budget of the requested expert q evenly across the τ time steps: 

γt,q = 1

τ , βt+1 ,q = 1 − tτ , t ∈ [τ ] .

For every inactive expert j̸ = q, the strategy places no dual mass and uses the first step only to increase the β-value by 1 /k :

γt,j = 0 , β t+1 ,j = min 



1, β 1,j + 1

k



, t = 1 , . . . , τ . 

The increase is made feasible by setting: 

α1 = 1

k , αt = 0 t ∈ [2 , τ ] ,

and the corresponding objective penalty is kα 1 = 1. Since γt,i ≤ βt,i − βt+1 ,i + αt is satisfied for every time step t ∈ [τ ] and every expert i ∈ [m], this assignment of the dual variables is feasible. The offline phase value is at least 

τ bf   1

τ

 − kα 1 = 2 √τ − 2 ≥ √τ . 

Proof of Theorem 5.1. We need to prove that for every constant C > 0, there exists an adaptive request sequence σ such that 

Dual ⋆(σ) ≥√ln k

32 Dual (σ) + C . 

The adversary repeatedly requests for R = ⌈C⌉ phases where each phase is constructed adap-tively following the single phase instance. The requested expert in each phase follows the fixed cyclic order: 1, 2, . . . , k + 1 , 1, 2, . . . . 

For each single phase r, let Dr be the online dual value accumulated during that phase, and let D⋆r be the offline optimal value. At the beginning, β1 = 1. For each phase r, we lower-bound the offline optimum using the strategy of Lemma 5.3. The offline strategy uses the first step of each phase to increase every non-requested expert’s β-value by 1 /k , capped at 1. Therefore, after an expert q is requested, it is not requested again for the next k phases. During these k phases, 

q receives k increments of its β-value with size 1 /k . Hence, by the next time q is requested, its 

β-value has returned to 1. Thus the strategy is feasible for the multiple-phase instance. Combining with Lemma 5.2, we obtain D⋆r ≥  

> √ln k
> 16

Dr and D⋆r ≥ 2. Averaging these two bounds gives 

D⋆r ≥√ln k

32 Dr + 1 .

Summing up R phases, we obtain 

Dual ⋆(σ) ≥√ln k

32 Dual (σ) + C . 

This completes the proof. 13 6 Static Model 

In the Static MoE Serving problem, the configuration remains fixed across all time steps, meaning 

xt = x ∈ Zm 

> ≥0

for all t ∈ [T ]. The primal convex program simplifies to minimizing the total latency cost P  

> t∈[T]

ft(x) subject to P 

> i∈[m]

xi = k and xi ≥ 0. That is: minimize X 

> t∈[T]

ft(x)subject to X

> i∈[m]

xi = kxi ≥ 0 ∀i ∈ [m]Let x⋆ ∈ Rm 

> ≥0

denote the optimal fractional solution to this static relaxation. 

6.1 Rounding Algorithm and Approximation Ratio 

We propose a simple deterministic rounding algorithm to convert the fractional optimal solution 

x⋆ into a feasible integral configuration. 

Rounding Algorithm 

• Solve the static convex program relaxation to obtain the optimal fractional configura-tion x⋆ ∈ Rm

> ≥0

.

• For each expert i ∈ [m], round down the allocation to construct an integral configura-tion ¯ xi = ⌊x⋆i ⌋.

• Allocate the remaining k − P 

> i∈[m]

¯xi arbitrarily. 

Theorem 6.1. The Rounding Algorithm is a 2-approximation for the Static MoE Serving problem. Proof. Let ¯ x ∈ Zm 

> ≥0

be the configuration produced by the algorithm. By construction, ¯ x is a feasible configuration. For any time step t ∈ [T ], the latency cost under configuration ¯ x is: 

ft(¯ x) = max 

> i∈[m]

rt,i 

1 + ¯ xi

≤ max 

> i∈[m]

rt,i 

1 + ⌊x⋆i ⌋ .

Since 1 + x ≤ 2(1 + ⌊x⌋), the above is at most: 2 · max 

> i∈[m]

rt,i 

1 + x⋆i

= 2 · ft(x⋆) .

Summing over t ∈ [T ], we have ALG ≤ 2 · Primal ≤ 2 · OPT .

6.2 Integrality Gap 

Next, we demonstrate that the above rounding for the convex program relaxation is tight, as the integrality gap approaches exactly 2 in the worst case. 

Theorem 6.2. The integrality gap of the static convex program relaxation is at least 2 − 1 

> m

, which approaches 2 as m → ∞ .

14 Proof. Consider an instance with m experts and n = 2 m − 1 total GPUs, which means the budget is k = m − 1. Let there be a single time step ( T = 1) with a uniform workload vector r1,i = 1 for all i ∈ [m]. For the fractional relaxation, a symmetric allocation evenly distributes the budget: x⋆i = km =1 − 1 

> m

for all i ∈ [m]. The latency cost of this fractional solution is: 

Primal ≤ f1(x⋆) = 11 +  1 − 1

> m

 = m

2m − 1 .

Now, consider any feasible integral configuration x ∈ Zm

> ≥0

. By the Pigeonhole Principle, at least one expert i∗ ∈ [m] must receive zero additional GPUs beyond the first one, meaning xi∗ = 0. The latency cost is bottlenecked by the expert with the minimum allocation: 

OPT ≥ 11 + xi∗

= 1 .

The integrality gap is then at least 2m−1 

> m

= 2 − 1 

> m

as stated in the theorem. 

6.3 Hardness of Approximation 

Having established a 2-approximation and a matching integrality gap for the static convex program relaxation, a natural theoretical question is whether we can bypass this relaxation barrier to achieve an arbitrarily good approximation. We answer this negatively. More precisely, we rule out the possibility of a Fully Polynomial-Time Approximation Scheme (FPTAS) under the Exponential Time Hypothesis (ETH). Our reduction relies on the Densest k-Subgraph (DkS) problem: 

Given an undirected graph G = ( V, E ) and an integer k ≤ | V |, find a subset of vertices 

S ⊆ V of size k that maximizes the number of induced edges |E(S)|.

The hardness of DkS has been extensively studied. For a vertex subset S with |S| = k, the 

density of the induced subgraph is |E(S)|/ k

> 2

 ∈ [0 , 1], so a k-clique has density 1. We leverage the following inapproximability result. 

Lemma 6.3 (40, Theorem 1) . Assuming ETH, there is a constant c > 0 such that no polynomial-time algorithm can distinguish between the following two cases for a graph G with m vertices and a positive integer k ≤ m:1. Clique: There exists a k-clique. 2. Sparse: Every k-subgraph of G has density at most δ = m−1/(log log m)c

.

Using this result, we next establish the hardness of the Static MoE Serving problem. 

Theorem 6.4. The Static MoE Serving problem is NP-hard. Furthermore, assuming the Expo-nential Time Hypothesis (ETH), it does not admit an FPTAS. Proof. We prove this by a reduction from DkS. Given an instance ( G, k ) of DkS where G = ( V, E )and m = |V |, we construct an instance of Static MoE Serving as follows: 

• Experts: Let there be an expert for each vertex of G. There are m = |V | experts. 

• Resources: Let the total number of GPUs be n = m + k.15 • Requests: Let there be a time step for each edge e = ( u, v ) ∈ E. We will abuse notation and refer to the time step also as e. Let the workload vector of step e be: 

re,i =

(

2 if i ∈ { u, v };1 otherwise. Let x ∈ Zm 

> ≥0

be a feasible configuration, i.e., P 

> i∈[m]

xi = k. We may assume without loss of generality that xi ∈ { 0, 1} for all i. This is because assigning xi ≥ 2 does not further decrease the objective, since the maximum workload is 2 — the latency cost would be bottlenecked by an expert without additional GPUs. Let S = {i | xi = 1 } be the set of experts receiving an extra GPU. By the budget constraint, 

|S| = k. The latency cost for a step e = ( u, v ) is: 

fe(x) = max 

> i∈[m]

re,i 

1 + xi

.

If both endpoints are selected, i.e., xu = xv = 1, we have re,u  

> 1+ xu

= re,v  

> 1+ xv

= 22 = 1. For any other expert w / ∈ { u, v }, the cost is at most 11+0 = 1. Thus, fe(x) = 1. By contrast, if at least one endpoint is not selected, the maximum evaluates to 21+0 = 2, yielding fe(x) = 2. Therefore, the total objective value is determined by the number of induced edges |E(S)|:

ALG (S) = 1 · | E(S)| + 2 · (|E| − | E(S)|) = 2|E| − | E(S)| .

NP-hardness. Minimizing the Static MoE cost is exactly equivalent to maximizing |E(S)|, the DkS objective. Since DkS is NP-hard, Static MoE Serving is also NP-hard. 

Impossibility of FPTAS. Mapping the hardness of DkS from Lemma 6.3 to our problem, the optimal cost OPT behaves as follows: 

• In the Clique case, the optimal cost is OPT ≤ 2|E| −  k

> 2

.

• In the Sparse case, the optimal cost is OPT ≥ 2|E| − δ k

> 2

.Suppose, for contradiction, that there is an FPTAS for Static MoE Serving. For any ϵ > 0, the FPTAS produces a solution with cost at most (1 + ϵ)OPT in poly( m, 1/ϵ ) time. To successfully distinguish the two cases in polynomial time, we only need to choose ϵ small enough such that the approximation intervals of the two cases do not overlap. That is, the worst-case approximation of the Clique case must be strictly less than the best-case cost of the Sparse case: (1 + ϵ)



2|E| − 

k

2

 

< 2|E| − δ

k

2



.

This inequality simplifies to: 2ϵ|E| < (1 + ϵ − δ)

k

2



.

Since the graph has m vertices, the total number of edges is bounded by |E| ≤  m

> 2

. Furthermore, 

δ = o(1) ≤ 12 for sufficiently large m. Therefore, it suffices to choose an ϵ satisfying: 

ϵ ≤ k(k − 1) 4m(m − 1) .

This means that 1 /ϵ is polynomially bounded by O(m2/k 2). Consequently, the running time of the FPTAS would be polynomial in m, giving us a polynomial-time algorithm to distinguish the Clique case from the Sparse case. This contradicts the hardness result of Manurangsi [40]. 16 A Deferred Proof from Section 3 

A.1 Proof of Lemma 3.1 

The integer constraints in the original problem require xt,i ∈ Z≥0 and P 

> i

xt,i = k. The convex program relaxes these requirements to xt,i ∈ R≥0 and P 

> i

xt,i ≤ k. Since the objective function ft

is convex and the constraints are linear, the minimum over the continuous relaxed domain is less than or equal to the minimum over the discrete integer domain. 

A.2 Proof of Lemma 3.3 

(Conjugate pair) Let At(xt) be the set of active coordinates attaining the maximum in ft(xt). By the subgradient convention above, every pt ∈ ∂f t(xt) can be written as 

pt,i = − X

> j∈At(xt)

λj

rt,j 

(1 + xt,j )2 1{i=j} ,

where λj ≥ 0 and P 

> j∈At(xt)

λj = 1. If γt = −pt, then: 

⟨γt, x t + 1⟩ = X

> j∈At(xt)

λj

rt,j 

1 + xt,j 

= X

> j∈At(xt)

λj ft(xt) = ft(xt) .

(Smoothness of ft) Suppose that θ ≥ 1 and xt,i + 1 ≤ θ(x′ 

> t,i

+ 1) for every i ∈ [m]. Then 

rt,i 

1 + x′

> t,i

≤ θ rt,i 

1 + xt,i 

∀i ∈ [m].

Taking the maximum over i gives ft(x′

> t

) ≤ θf t(xt). 

(Sublinearity of ˆft) By the form of ft and the signed Fenchel transform: ˆft(γt) = inf  

> xt≥0

max 

> i∈[m]

rt,i 

1 + xt,i 

+ X

> i

γt,i xt,i 

!

.

Introduce an epigraph variable τ for the maximum term. Then, the infimum for a fixed τ is achieved when we let xt,i be as small as possible subject to τ ≥ rt,i /(1 + xt,i ) for every i. Thus: ˆft(γt) = inf 

> τ > 0

"

τ + X

> i

γt,i 

 rt,i 

τ − 1



> +

#

.

Fix θ ∈ (0 , 1]. For any τ = √θs with s > 0, we have: 

τ + X

> i

θγ t,i 

 rt,i 

τ − 1



> +

= √θs + θ X

> i

γt,i 

 rt,i 

√θs − 1



> +

= √θ

"

s + X

> i

γt,i 

 rt,i 

s − √θ



> +

#

.

Since θ ∈ (0 , 1], the term between the brackets is at least: 

s + X

> i

γt,i 

 rt,i 

s − 1



> +

≥ ˆft(γt) .

17 We conclude that: 

τ + X

> i

θγ t,i 

 rt,i 

τ − 1



> +

≥ √θ ˆft(γt) .

Taking infimum of the left-hand side over τ > 0 proves ˆft(θγ t) ≥ √θ ˆft(γt). 

A.3 Derivation of the Offset Fenchel Dual and Proof of Lemma 3.4 

We derive the dual program in Section 3 and prove the additive weak-duality bound in Lemma 3.4. For each budget constraint, introduce a multiplier αt ∈ R≥0. For each incremental reconfiguration constraint xt,i − xt−1,i − yt,i ≤ 0, introduce a multiplier βt,i ≥ 0. We also introduce an auxiliary terminal multiplier βT +1 ,i ≥ 0 for the domain constraint −xT,i ≤ 0; this variable lets us write the dynamic terms uniformly. Finally, introduce γt ∈ Rm 

> ≥0

as a Fenchel variable and use: 

ft(xt) = sup 

> γt∈Rm
> ≥0

  ˆft(γt) − ⟨ xt, γ t⟩ .

The corresponding Lagrange lower bound is 

L(x, y ; α, β, γ ) = X 

> t∈[T]

 ˆft(γt) − ⟨ xt, γ t⟩



+ X 

> t∈[T]

X

> i∈[m]

yt,i + X 

> t∈[T]

αt

 X 

> i∈[m]

xt,i − k



+ X 

> t∈[T]

X

> i∈[m]

βt,i 

 xt,i − xt−1,i − yt,i 

 − X

> i∈[m]

βT +1 ,i xT,i .

For every feasible ( x, y ), the equality term vanishes, the inequality terms are non-positive, and ˆft(γt) − ⟨ xt, γ t⟩ ≤ ft(xt) by definition. Hence, inf x,y ≥0 L(x, y ; α, β, γ ) is a lower bound on Primal ⋆

for all α ≥ 0, β ≥ 0, and γ ≥ 0. Grouping terms by the primal variables gives 

L(x, y ; α, β, γ ) = X 

> t∈[T]

ˆft(γt) + X 

> t∈[T]

X

> i∈[m]

xt,i 

 αt + βt,i − βt+1 ,i − γt,i 



+ X 

> t∈[T]

X

> i∈[m]

yt,i 

 1 − βt,i 



− X 

> t∈[T]

kα t − ⟨ x0, β 1⟩ .

The minimization over xt,i ≥ 0 and yt,i ≥ 0 yields dual constraints: 

γt,i + βt+1 ,i − βt,i ≤ αt , γt,i ≥ 0 , 0 ≤ βt,i ≤ 1 .

The resulting Fenchel dual program is: maximize X 

> t∈[T]

ˆft(γt) − X 

> t∈[T]

kα t − ⟨ x0, β 1⟩

subject to γt,i + βt+1 ,i − βt,i ≤ αt ∀t ∈ [T ], ∀i ∈ [m]0 ≤ βt,i ≤ 1 ∀t ∈ [T + 1] , ∀i ∈ [m]

γt,i ≥ 0 ∀t ∈ [T ], ∀i ∈ [m]

αt ≥ 0 ∀t ∈ [T ] .

18 By weak duality, the optimal value of this dual is at most Primal ⋆. The dual program in Section 3 omits the term −⟨ x0, β 1⟩ from the objective. By 0 ≤ β1,i ≤ 1 for all i and ∥x0∥1 = k, we have 0 ≤ ⟨ x0, β 1⟩ ≤ k. Hence, its optimal value Dual ⋆ is at most the optimal of the above dual plus k. This gives Dual ⋆ ≤ Primal ⋆ + k.

# B Deferred Proofs from Section 4 

B.1 Proof of Lemma 4.3 

First, we bound the incremental reconfiguration cost using the inequality u−v ≤ u ln uv for u, v > 0: 

xt,i − xt−1,i ≤ (xt,i + 1) ln xt,i + 1 

xt−1,i + 1 .

Summing over all i where the allocation strictly increases (i.e., xt,i > x t−1,i ≥ 0): 

X

> i

yt,i ≤ X  

> i:xt,i >x t−1,i

(xt,i + 1) ln xt,i + 1 

xt−1,i + 1 .

By Equation (2) with equality (since xt,i > 0) and the relation between αt, γ t,i and α′

> t

, γ ′

> t,i

:

X

> i

yt,i ≤ η X  

> i:xt,i >x t−1,i

(xt,i + 1)( γt,i − αt)Dropping αt ≥ 0 and expanding the summation to all i ∈ [m] (since γt,i ≥ 0), we have: 

X

> i

yt,i ≤ η X

> i∈[m]

(xt,i + 1) γt,i .

Again by the relation between γt,i and γ′

> t,i

, we have: 

X

> i

yt,i ≤ X

> i∈[m]

(xt,i + 1) γ′ 

> t,i

= ft(xt) ,

where the last equality follows by Lemma 3.3. 

# References 

[1] Sandhini Agarwal, Lama Ahmad, Jason Ai, Sam Altman, Andy Applebaum, Edwin Arbus, Rahul K Arora, Yu Bai, Bowen Baker, Haiming Bao, et al. gpt-oss-120b & gpt-oss-20b model card. arXiv preprint arXiv:2508.10925 , 2025. [2] Amey Agrawal, Nitin Kedia, Ashish Panwar, Jayashree Mohan, Nipun Kwatra, Bhargav Gula-vani, Alexey Tumanov, and Ramachandran Ramjee. Taming Throughput-Latency tradeoff in LLM inference with Sarathi-Serve. In 18th USENIX symposium on operating systems design and implementation (OSDI 24) , pages 117–134, 2024. [3] Lachlan Andrew, Siddharth Barman, Katrina Ligett, Minghong Lin, Adam Meyerson, Alan Roytman, and Adam Wierman. A tale of two metrics: Simultaneous bounds on competitiveness and regret. In Conference on Learning Theory , pages 741–763. PMLR, 2013. 19 [4] CJ Argue, Anupam Gupta, and Guru Guruganesh. Dimension-free bounds for chasing convex functions. In Conference on Learning Theory , pages 219–241. PMLR, 2020. [5] Yossi Azar, Niv Buchbinder, TH Hubert Chan, Shahar Chen, Ilan Reuven Cohen, Anupam Gupta, Zhiyi Huang, Ning Kang, Viswanath Nagarajan, Joseph Naor, et al. Online algo-rithms for covering and packing problems with convex objectives. In 2016 IEEE 57th Annual Symposium on Foundations of Computer Science (FOCS) , pages 148–157. IEEE, 2016. [6] Nikhil Bansal, Niv Buchbinder, and Joseph Naor. Randomized competitive algorithms for generalized caching. SIAM Journal on Computing , 41(2):391–414, 2012. [7] Nikhil Bansal, Anupam Gupta, Ravishankar Krishnaswamy, Kirk Pruhs, Kevin Schewior, and Cliff Stein. A 2-competitive algorithm for online convex optimization with switching costs. In 

Approximation, Randomization, and Combinatorial Optimization. Algorithms and Techniques (APPROX/RANDOM 2015) , pages 96–109. Schloss Dagstuhl–Leibniz-Zentrum f¨ ur Informatik, 2015. [8] Yair Bartal, Avrim Blum, Carl Burch, and Andrew Tomkins. A polylog (n)-competitive algo-rithm for metrical task systems. In Proceedings of the twenty-ninth annual ACM symposium on Theory of computing , pages 711–719, 1997. [9] Luca Becchetti and Stefano Leonardi. Nonclairvoyant scheduling to minimize the total flow time on single and parallel machines. Journal of the ACM (JACM) , 51(4):517–539, 2004. [10] Avrim Blum, Carl Burch, and Adam Kalai. Finely-competitive paging. In 40th Annual Sym-posium on Foundations of Computer Science (Cat. No. 99CB37039) , pages 450–457. IEEE, 1999. [11] Allan Borodin, Nathan Linial, and Michael E Saks. An optimal on-line algorithm for metrical task system. Journal of the ACM (JACM) , 39(4):745–763, 1992. [12] S´ ebastien Bubeck, Michael B Cohen, Yin Tat Lee, James R Lee, and Aleksander Madry. K-server via multiscale entropic regularization. In Proceedings of the 50th annual ACM SIGACT symposium on theory of computing , pages 3–16, 2018. [13] S´ ebastien Bubeck, Yin Tat Lee, Yuanzhi Li, and Mark Sellke. Competitively chasing convex bodies. In Proceedings of the 51st Annual ACM SIGACT Symposium on Theory of Computing ,pages 861–868, 2019. [14] Niv Buchbinder, Shahar Chen, and Joseph Naor. Competitive analysis via regularization. In 

Proceedings of the twenty-fifth annual ACM-SIAM symposium on Discrete algorithms , pages 436–444. SIAM, 2014. [15] Niv Buchbinder, Anupam Gupta, Marco Molinaro, and Joseph Naor. k-servers with a smile: Online algorithms via projections. In Proceedings of the Thirtieth Annual ACM-SIAM Sym-posium on Discrete Algorithms , pages 98–116. SIAM, 2019. [16] Vijaya Chandru, C-Y Lee, and Reha Uzsoy. Minimizing total completion time on batch processing machines. The International Journal Of Production Research , 31(9):2097–2121, 1993. 20 [17] Niangjun Chen, Gautam Goel, and Adam Wierman. Smoothed online convex optimization in high dimensions via online balanced descent. In Conference On Learning Theory , pages 1574–1594. PMLR, 2018. [18] Christian Coester and James R Lee. Pure entropic regularization for metrical task systems. In Conference on Learning Theory , pages 835–848. PMLR, 2019. [19] Damai Dai, Chengqi Deng, Chenggang Zhao, RX Xu, Huazuo Gao, Deli Chen, Jiashi Li, Wangding Zeng, Xingkai Yu, Yu Wu, et al. Deepseekmoe: Towards ultimate expert specializa-tion in mixture-of-experts language models. In Proceedings of the 62nd Annual Meeting of the Association for Computational Linguistics (Volume 1: Long Papers) , pages 1280–1297, 2024. [20] Nikhil R Devanur and Zhiyi Huang. Primal dual gives almost optimal energy efficient online algorithms. In Proceedings of the twenty-fifth annual ACM-SIAM symposium on Discrete algorithms , pages 1123–1140, 2014. [21] Nikhil R Devanur and Kamal Jain. Online matching with concave returns. In Proceedings of the forty-fourth annual ACM symposium on Theory of computing , pages 137–144, 2012. [22] William Fedus, Barret Zoph, and Noam Shazeer. Switch transformers: Scaling to trillion parameter models with simple and efficient sparsity. Journal of Machine Learning Research ,23(120):1–39, 2022. [23] Amos Fiat, Richard M Karp, Michael Luby, Lyle A McGeoch, Daniel D Sleator, and Neal E Young. Competitive paging algorithms. Journal of Algorithms , 12(4):685–699, 1991. [24] Joel Friedman and Nathan Linial. On convex body chasing. Discrete & Computational Ge-ometry , 9(3):293–321, 1993. [25] Seokjin Go and Divya Mahajan. Moetuner: Optimized mixture of expert serving with balanced expert placement and token routing. arXiv preprint arXiv:2502.06643 , 2025. [26] Google DeepMind. Gemini 3 Pro model card, November 2025. Model card; accessed July 2026. [27] Dorit S Hochbaum and Dan Landy. Scheduling semiconductor burn-in operations to minimize total flowtime. Operations research , 45(6):874–885, 1997. [28] Zhiyi Huang and Anthony Kim. Welfare maximization with production costs: A primal dual approach. In Proceedings of the Twenty-Sixth Annual ACM-SIAM Symposium on Discrete Algorithms , pages 59–72. SIAM, 2014. [29] Robert A Jacobs, Michael I Jordan, Steven J Nowlan, and Geoffrey E Hinton. Adaptive mixtures of local experts. Neural computation , 3(1):79–87, 1991. [30] Patrick Jaillet, Jiashuo Jiang, Konstantina Mellou, Marco Molinaro, Chara Podimata, and Zijie Zhou. Online scheduling for llm inference with kv cache constraints. arXiv preprint arXiv:2502.07115 , 2025. [31] Albert Q Jiang, Alexandre Sablayrolles, Antoine Roux, Arthur Mensch, Blanche Savary, Chris Bamford, Devendra Singh Chaplot, Diego de las Casas, Emma Bou Hanna, Florian Bressand, et al. Mixtral of experts. arXiv preprint arXiv:2401.04088 , 2024. [32] Michael I Jordan and Robert A Jacobs. Hierarchical mixtures of experts and the em algorithm. 

Neural computation , 6(2):181–214, 1994. 21 [33] Bala Kalyanasundaram and Kirk Pruhs. Speed is as powerful as clairvoyance. Journal of the ACM (JACM) , 47(4):617–643, 2000. [34] Li Kong, Qi Qi, Yinyu Ye, and Zijie Zhou. Geometry-aware online scheduling for llm serving: From theoretical bound to system practice. arXiv preprint arXiv:2606.22327 , 2026. [35] Elias Koutsoupias and Christos H. Papadimitriou. On the k-server conjecture. Journal of the ACM , 42(5):971–983, 1995. [36] Woosuk Kwon, Zhuohan Li, Siyuan Zhuang, Ying Sheng, Lianmin Zheng, Cody Hao Yu, Joseph Gonzalez, Hao Zhang, and Ion Stoica. Efficient memory management for large language model serving with pagedattention. In Proceedings of the 29th symposium on operating systems principles , pages 611–626, 2023. [37] Jiamin Li, Yimin Jiang, Yibo Zhu, Cong Wang, and Hong Xu. Accelerating distributed MoE training and inference with lina. In 2023 USENIX Annual Technical Conference (USENIX ATC 23) , pages 945–959, 2023. [38] Aixin Liu, Bei Feng, Bing Xue, Bingxuan Wang, Bochao Wu, Chengda Lu, Chenggang Zhao, Chengqi Deng, Chenyu Zhang, Chong Ruan, et al. Deepseek-v3 technical report. arXiv preprint arXiv:2412.19437 , 2024. [39] Mark S. Manasse, Lyle A. McGeoch, and Daniel D. Sleator. Competitive algorithms for server problems. Journal of Algorithms , 11(2):208–230, 1990. [40] Pasin Manurangsi. Almost-polynomial ratio eth-hardness of approximating densest k-subgraph. In Proceedings of the 49th Annual ACM SIGACT Symposium on Theory of Com-puting , pages 954–961, 2017. [41] Rajeev Motwani, Steven Phillips, and Eric Torng. Nonclairvoyant scheduling. Theoretical computer science , 130(1):17–47, 1994. [42] Mark Sellke. Chasing convex bodies optimally. In Proceedings of the Fourteenth Annual ACM-SIAM Symposium on Discrete Algorithms , pages 1509–1518. SIAM, 2020. [43] Noam Shazeer, Azalia Mirhoseini, Krzysztof Maziarz, Andy Davis, Quoc Le, Geoffrey Hinton, and Jeff Dean. Outrageously large neural networks: The sparsely-gated mixture-of-experts layer. In International Conference on Learning Representations , 2017. [44] Daniel D Sleator and Robert E Tarjan. Amortized efficiency of list update and paging rules. 

Communications of the ACM , 28(2):202–208, 1985. [45] Reha Uzsoy. Scheduling a single batch processing machine with non-identical job sizes. The International Journal of Production Research , 32(7):1615–1635, 1994. [46] Meixuan Wang, Yinyu Ye, and Zijie Zhou. Llm serving optimization with variable prefill and decode lengths. arXiv preprint arXiv:2508.06133 , 2025. [47] Bingyang Wu, Yinmin Zhong, Zili Zhang, Shengyu Liu, Fangyue Liu, Yuanhang Sun, Gang Huang, Xuanzhe Liu, and Xin Jin. FastServe:Iteration-Level preemptive scheduling for large language model inference. In 23rd USENIX Symposium on Networked Systems Design and Implementation (NSDI 26) , pages 57–74, 2026. 22 [48] Gyeong-In Yu, Joo Seong Jeong, Geon-Woo Kim, Soojeong Kim, and Byung-Gon Chun. Orca: A distributed serving system for Transformer-Based generative models. In 16th USENIX symposium on operating systems design and implementation (OSDI 22) , pages 521–538, 2022. 23