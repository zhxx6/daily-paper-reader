---
title: "Route-Block Membership Selects Packed-AWQ Arithmetic: A Controlled Single-Fixture Mechanism Study"
title_zh: 路由块成员选择打包AWQ算术：一项受控单夹具机制研究
authors: Lukas Stepanek
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.26316v1"
tags: ["query:moe-gk-quant"]
score: 10.0
evidence: MoE推理中的打包量化矩阵乘法和路由块对齐
tldr: MoE推理中路由token打包对齐通常被视为记账步骤。本研究在Qwen3-Coder AWQ层固定装置上，通过route-block干预选择packed算术轨迹。两种预构造历史产生不同对齐和轨迹，交换路由可转移完整轨迹，强制单切片网格使W13位相等，稳定规范构造使两者收敛到第三轨迹。该结果确认了因果机制，但不声明普遍性或可移植性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.26316v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1066, \"height\": 465, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26316v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 992, \"height\": 481, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.26316v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1383, \"height\": 382, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26316v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1175, \"height\": 381, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26316v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1101, \"height\": 184, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26316v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1380, \"height\": 524, \"label\": \"Table\"}]"
motivation: 探究MoE推理中路由块成员选择是否影响packed量化矩阵乘法的精确算术路径。
method: 在Qwen3-Coder AWQ第6层固定装置上，使用vLLM/Marlin和RTX 3090，通过route-block干预对比两种预构造历史。
result: 两种历史产生不同对齐和轨迹；交换块间路由转移完整轨迹；强制单切片网格使W13位相等；规范构造收敛到第三轨迹。
conclusion: 路由块干预可精确选择packed算术轨迹，但结果局限于单一装置，不声明普遍性。
---

## 摘要
混合专家（MoE）推理首先将路由令牌对齐到填充的专家块中，然后对这些块执行打包的量化矩阵乘法。这种预处理通常被视为簿记操作。在一个预先指定的Qwen3-Coder AWQ第6层夹具上，使用固定的vLLM/Marlin构建和RTX 3090运行时，我们展示了测试的路由块干预选择了精确的打包算术轨迹。两个固定的预构建历史产生了不同的原生对齐和精确轨迹。注入相反的对齐方式转移了W13、激活值、路由W2和最终输出。在一个块内交换两个路由保持了每个原生轨迹，而在专家-106块40和41的边界上交换两个先前数据选择的路由则转移了完整的相反轨迹。源和二值衍生的调度几何将这些块映射到直接/全K和分裂/全局归约类别。强制使用单切片200块网格使W13逐位相等。稳定的规范构建使两个历史收敛到第三个精确轨迹。确认队列包含70个有效冷过程和7个必要扰动拒绝。这是一个针对单一夹具的因果机制结果，而非关于流行性、分配器、可移植性或服务影响的声明。

## Abstract
Mixture-of-experts (MoE) inference first aligns routed tokens into padded expert blocks, then executes packed quantized matrix multiplication over those blocks. This preprocessing is often treated as bookkeeping. In one pre-specified Qwen3-Coder AWQ layer-6 fixture on a pinned vLLM/Marlin build and RTX 3090 runtime, we show that the tested route-block interventions select exact packed arithmetic trajectories. Two fixed preconstruction histories produced distinct native alignments and exact trajectories. Injecting the opposite alignment transferred W13, activation, routed-W2, and final outputs. Permuting two routes within one block preserved each native trajectory, while exchanging two prior-data-selected routes across the boundary between expert-106 blocks 40 and 41 transferred the complete opposite trajectory. Source- and binary-derived schedule geometry maps those blocks to direct/full-K and split/global-reduction classes. Forcing a single-slice 200-block grid made W13 bitwise equal. Stable canonical construction made both histories converge to a third exact trajectory. The confirmatory cohort contains 70 valid cold processes and seven required perturbation rejections. This is a causal mechanism result for one fixture, not a prevalence, allocator, portability, or serving-impact claim.

---

## 论文详细总结（自动生成）

# 论文结构化总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在混合专家（MoE）推理中，路由令牌经过动态路由后被分组并对齐到填充的专家块中，然后对这些块执行打包的量化矩阵乘法。这种对齐过程通常被视为无关紧要的簿记操作。但本论文试图回答：**路由块成员身份（即某一路由属于哪一个八行专家块）是否会直接影响打包算术的精确数值轨迹（即浮点加法的非结合性导致的舍入路径差异）？**
- **整体含义**：通过一个高度受控的单一夹具实验，作者证明**路由块边界可以充当算术控制流**——改变路由的块成员身份会改变其进入的归约调度类别（直接/全K 或 分裂/全局归约），从而转移整个前向传播的精确数值输出。该结果澄清了此前仅在工程层面观察到的输出差异背后的因果机制，但明确声明不适用于流行性、分配器、可移植性或服务影响。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：建立一个抽象概念——**路由块成员（route-block membership）**，即对于每个专家 e 的填充对齐序列，连续八个位置组成一个本地块 B(e, j)，包含所有有效路由标识符（忽略块内顺序）。改变路由跨越八行块边界（即从 B(e, j) 移动到 B(e, j+1)）即改变块成员，进而改变底层 GPU 归约调度。
- **关键技术细节**：
    - **固定夹具**：使用 Qwen3-Coder-30B-A3B-Instruct-AWQ 检查点的第 6 层，输入维度 N=1536，K=2048，块大小 b=8，M=21 个 token，k=8，共 168 个有效路由，填充后产生 50 个活动专家块（400 对齐行）。
    - **调度类别推导**：通过分析 vLLM/Marlin 内核的自动选择器（自动选择线程块网格），在原生 246 个 CUDA 块网格下，前 492 个全局块（对应 MoE 块 0~40）获得直接/全 K 归约；从块 41 开始的剩余部分采用分裂 K（两片 16 K-tile）加全局 FP32 归约。因此块 40 和块 41 属于不同的调度类别。
    - **两种预构造历史**：H0（无额外分配/预热）、H1（模型构建前分配并保留三个 CUDA 张量但从不用于计算）——它们产生不同的原生对齐顺序。
    - **干预实验**：通过注入“本历史的对齐计划”或“相反历史的对齐计划”，以及块内置换（路线 22/28）和块间边界交换（路线 51/86）等手术式干预，精确测量四个数值表面（W13 权重、激活值、路由-W2、最终输出）的哈希值。
    - **调度控制**：强制使用 200 个 CUDA 块的网格，使所有列 tile 变为单切片，绕过全局归约，预测 W13 会变为逐位相等。
    - **规范构造**：稳定地按照（专家 ID, 扁平化路由 ID）排序构建对齐，预测两个历史将收敛到第三个精确轨迹。

## 3. 实验设计：使用了哪些数据集 / 场景，benchmark，对比了哪些方法

- **数据集/场景**：未使用公开数据集；使用来自单一提示（prompt）的固定输入张量、top-k 路由 ID 和权重（私有提示，未公开）。该夹具是第 6 层的隐藏状态。
- **Benchmark**：无标准化 benchmark；论文自行定义了“精确轨迹”哈希比对（W13、激活、W2、最终输出）。还报告了单点性能（层调用时间和批量 1 固定提示令牌/秒），但未用作精确性验证。
- **对比方法**：
    - 两个原生历史 H0 vs. H1 的轨迹对比。
    - 同历史重复验证（own order）以排除干预的影响。
    - 交叉历史注入（cross order）以证明完全转移。
    - 块内置换（within-block）和块间边界交换（boundary）以分离关键路由。
    - 网格 200 控制（schedule control）以验证调度类别解释。
    - 规范构造（canonical）与两个原生历史的对比。
    - 部分历史对照实验（historical corroboration）验证了 H0/H1 的稳定性，但非正式队列。
    - 与相关工作的概念对比（AWQ、Marlin、MegaBlocks、UniEP、Bit-Exact、vLLM PR 46639/48032）。

## 4. 资源与算力

- **GPU**：一张 **NVIDIA RTX 3090**（计算能力 8.6）。
- **软件**：驱动 580.159.03，CUDA 13.0，PyTorch 2.11.0+cu130，vLLM 固定提交 752a3a504485790a2e8491cacbb35c137339ad34。
- **训练时长**：**未涉及训练**，仅推理阶段冷过程测量。每个过程（process）是独立的冷启动（cold process），共 70 个有效冷过程 + 7 个拒绝扰动 + 额外规范构造评估的 104+1024+480+30 个过程。未报告总运行时间。
- **算力说明**：论文明确指出不进行性能分析，仅使用单卡 RTX 3090，资源规模极小。

## 5. 实验数量与充分性

- **实验数量**：
    - 主要确认队列（confirmatory cohort）：70 个有效冷过程，分为 20 个资格/20 个因果转移/30 个消融，每细胞/历史五个重复。7 个扰动被拒绝（原因预声明）。
    - 规范构造评估：独立的 104 个随机对齐案例、1024 个固定向量回放、所有 48 个 MoE 层（480 次观测 in 10 processes）、30 个无钩冷过程。共 1638+ 次运行。
- **充分性**：
    - **优势**：严格的前瞻性设计（prespecified histories, interventions, prediction, rejection rules），复现按复制交替顺序执行，独立评分者验证，所有干预无条件满足预测。70 个冷过程完全符合，无任何失败。
    - **不足**：**仅有一个问题实例**（一个模型检查点、一层、一个提示衍生夹具、一个 GPU、一个 vLLM 版本）。5 个重复证明可重复性，但不保证外部推广。焦点路由（22/28, 51/86）是从先前数据中选择的，存在选择偏倚风险。实验设计是因果机制展示，而非广泛性测试。

## 6. 论文的主要结论与发现

- **核心结论**：**路由块成员身份精确选择打包算术轨迹**。两个原生历史 H0/H1 在四个测量表面上完全不同。注入相反计划完全转移了所有四个表面到另一轨迹。块内置换（22/28）保持轨迹不变；块间边界交换（51/86）足以使完整轨迹转移到对方。强制 200 块网格使 W13 逐位相等（确认调度类别解释）。规范构造使两个历史收敛到第三个独特轨迹。
- **数值差异极小**：在 W13 上仅 7 个 FP16 元素有差异（最大绝对值 7.6e-6），激活 3 个，W2 10 个，最终输出 1 个。这些差异科学上具有诊断性，但未测试对任务质量或生成 token 的实际影响。
- **结论边界**：声明是单一夹具的因果证据，不声称流行性、分配器机制、可移植性或服务影响。

## 7. 优点：方法或实验设计上的亮点

- **严格受控的前瞻性设计**：所有干预、预测、重复数量、拒绝规则均在执行前固定，并由独立评分者软件验证，避免了事后分析偏倚。
- **抽象简洁有效**：路由块成员这一抽象概念连接了动态调度与底层 GPU 归约调度，可解释性高。
- **手术式干预**：通过精确到两条路由的交换（51/86）就能产生完全转移，而块内置换不改变结果，清晰地分离了因果关系。
- **多重验证**：同历史注入（消除干预影响）、调度控制（网格 200）、规范构建等相互支持，形成了强因果链。
- **完全可审计**：公开了证据包（GitHub 仓库），包含冻结合同、SHA-256 校验、评分代码、独立验证器（CPU-only），但提示和中间张量因隐私未公开。
- **诚实透明的局限声明**：论文明确列出了实验局限（单一夹具、焦点路由选择、复合处理、无服务影响评估等），不夸大结论。

## 8. 不足与局限

- **实验覆盖严重不足**：仅测试了一个模型（Qwen3-Coder-30B-A3B）、单层（第 6 层）、一个提示衍生夹具、一个 GPU（RTX 3090）、一个 vLLM 版本、一个内核版本。无法推广至其他模型、层、输入、硬件或软件配置。
- **焦点路由选择偏倚**：关键干预的路由（22/28, 51/86）是从历史数据中挑选的，论文承认“我们不对流行性或分母进行估计”，因此存在选择性报告风险。
- **复合处理 H0/H1 不可拆分**：H1 同时进行了分配、预预热、地址改变等，不能归因于单一分配器行为。作者明确声明不标识分配器原语。
- **无实际影响力评估**：最终数字差异极小（1 个 FP16 元素），未测试对 token 生成质量、文本一致性或任务效率的任何影响。科学上有趣但实践意义未知。
- **二进制与软件构建差异**：70 过程队列和规范构造评估使用不同扩展哈希，虽然核心源代码（ops.cu, marlin_template.h）字节相同，但整体二进制不同，不能视为同一实验种群，削弱了联合证明力。
- **公开证据不可完整重现**：因私有提示和中间张量未公开，独立第三方无法精确复现报告中的比特模式，只能进行方法学复制。
- **无独立性时间戳**：论文说“合同和结果首次同时在执行后发布”，没有独立的第三方预注册时间戳，存在前后不一致的理论可能（但配套仓库提供了审计链接）。
- **性能数据极其有限**：单点计时（0.2412 vs 0.2416 ms）仅用于说明无严重退化，不能代表任何性能声明。

（完）
