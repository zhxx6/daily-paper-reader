---
title: "Variable Bit-width Quantization: Learning Per-Group Precision for \"Bigger-but-Smaller\" Language Models"
title_zh: 可变位宽量化：为“更大但更小”语言模型学习每组精度
authors: Hamish Ogilvy
date: 2026-07-03
pdf: "https://arxiv.org/pdf/2607.02893v1"
tags: ["query:moe-gk-quant"]
score: 9.0
evidence: 大语言模型每组变比特量化
tldr: "低比特量化通常为所有权重设置统一位宽，忽视了不同部分的敏感性差异。本文提出VBQ方法，让每组64个权重通过Gumbel-Softmax学习1、2、4、8比特的混合精度，并用交替优化训练。VBQ发现强非均匀分配：69%的组坍缩到1比特，平均1.82比特的131M模型在TinyStories上PPL 4.2，超越55M FP16模型（PPL 4.4），存储减少3.8倍；1.46B模型在FineWeb-Edu上匹配593M FP16，存储减少3.7倍，参数多2.5倍。VBQ在质量每字节上比FP16高效3.9-8.4倍，并带来推理加速。深度网络的后层能自愈量化误差，精度作为可学习非均匀资源，不均匀分配优于均匀分配。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.02893v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1612, \"height\": 627, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02893v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1610, \"height\": 637, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02893v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1061, \"height\": 634, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02893v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1055, \"height\": 713, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02893v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1080, \"height\": 640, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.02893v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1506, \"height\": 404, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02893v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1460, \"height\": 267, \"label\": \"Table\"}]"
motivation: 统一位宽量化忽略了不同权重对精度的需求差异，导致压缩效率次优。
method: "对每组64权重，用Gumbel-Softmax从{1,2,4,8}中学习最优位宽，通过交替优化联合训练精度logits和网络权重。"
result: "VBQ发现69%组坍缩至1比特，1.82平均比特的131M模型超越55M FP16；1.46B模型以3.7倍存储节约匹配593M FP16。"
conclusion: 不均匀分配比特预算比均匀分配更优，深度网络后层能自愈前层量化误差，实现更大但更小的语言模型。
---

## 摘要
低位量化缩小了语言模型，但将精度视为单一的全局超参数：每个权重使用相同的位宽。我们引入了可变位宽量化（VBQ），这是一种训练时方法，其中每个连续的64个权重组通过Gumbel-Softmax松弛从{1,2,4,8}位中学习自己的分辨率，并通过交替优化联合训练，为精度对数提供清晰、任务对齐的信号。VBQ在单个投影类型内发现了一致且高度异构的分配，而不仅仅是在层之间，这是逐层方法无法表达的：69%的组压缩到1位，语言模型头部平均1.09位，而第一个MLP块保持约2.5位。这种模式足够稳定，可以冻结成固定方案并重复使用，无需进一步搜索。该方案产生了一种“更大但更小”的模式：一个平均1.82位的1.31亿模型在TinyStories上达到困惑度4.2，以3.8倍的存储减少击败了1.55亿FP16模型（困惑度4.4），并让FineWeb-Edu上的14.6亿模型以约3.7倍的存储减少和2.5倍的参数数量匹配了5.93亿FP16对照模型。按每字节质量计算，VBQ比FP16高效3.9-8.4倍。该方案直接映射到打包的低位存储，因此也加速了推理：使用自定义融合解量化和乘法内核，在同等输出下，内存带宽受限的自回归解码更快，且加速比随规模增长（在Apple Silicon上，1.31亿时持平，10亿时1.9倍，90亿时4.7倍）。分布分析（KL散度和argmax翻转率）揭示了一个显著机制：深层逐步自愈早期层注入的量化误差。这一胜利是从头开始的训练时现象；将搜索经济地扩展到15亿参数以上仍待探索。VBQ将精度重新定义为一个可学习的非均匀资源，并表明在固定位预算下不均匀分配优于均匀分配。

## Abstract
Low-bit quantization shrinks language models but treats precision as a single global hyper-parameter: every weight uses the same bit-width. We introduce Variable Bit-width Quantization (VBQ), a training-time method in which each contiguous group of 64 weights learns its own resolution from {1,2,4,8} bits via a Gumbel-Softmax relaxation, trained jointly by an alternating optimization that gives the precision logits a clean, task-aligned signal. VBQ discovers a consistent, strongly heterogeneous allocation within individual projection types, not merely across layers, impossible to express with per-layer methods: 69% of groups collapse to 1 bit, the LM head averages 1.09 bits, while the first MLP block keeps ~2.5 bits. This pattern is stable enough to freeze into a fixed recipe and reuse without further search. The recipe yields a "bigger-but-smaller" regime: a 131M model at 1.82 mean bits reaches perplexity 4.2 on TinyStories, beating a 55M FP16 model (PPL 4.4) at 3.8x less storage, and lets a 1.46B model on FineWeb-Edu match a 593M FP16 control at ~3.7x less storage with 2.5x more parameters. As quality-per-byte, VBQ is 3.9-8.4x more efficient than FP16. The recipe maps directly to packed low-bit storage, so it also accelerates inference: with custom fused dequantize-and-multiply kernels, memory-bandwidth-bound autoregressive decode is faster at equal output, and the speedup grows with scale (parity at 131M, 1.9x at 1.0B, 4.7x at 9B on Apple silicon). A distributional analysis (KL divergence and argmax-flip rate) reveals a striking mechanism: deeper layers progressively self-heal the quantization error injected by early layers. The win is a from-scratch, train-time phenomenon; scaling the search economically beyond 1.5B parameters remains open. VBQ reframes precision as a learnable, non-uniform resource and shows that spending a fixed bit budget unevenly beats spending it uniformly.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机和背景）
- **问题**：现有低比特量化（如GPTQ、AWQ、BitNet）对所有权重使用统一的位宽，忽略了不同权重对精度的敏感性差异，导致压缩效率次优。
- **背景**：已有证据表明，语言模型的信息容量存在约 3.5–4 位/值的经验下限（如苹果设备模型、k-bit缩放律），但统一量化只能逼近该下限，无法进一步突破。
- **整体含义**：VBQ 将精度视为可学习的非均匀资源，通过“更大但更小”的策略——用更多参数但更低位宽的模型，在相同存储预算下获得更高质量——证明了非均匀分配比特预算优于均匀分配。

### 2. 方法论：核心思想、关键技术细节
- **核心思想**：将权重矩阵划分为连续的 64 元组，每个组通过 Gumbel-Softmax 松弛从候选位宽 {1,2,4,8} 中学习自己的位宽，并与网络权重联合优化。
- **关键技术细节**：
  - **对称 Absmax 量化**：每组计算尺度因子 \( s_g \)，使用直通估计器传递梯度。
  - **Gumbel-Softmax**：每组 logits 经过 Gumbel 噪声和温度退火（从 5.0 降至 0.5）得到软选择权重，再对候选重构权重进行凸组合。
  - **梯度隔离与交替优化（Algorithm 1）**：
    - 权重步：冻结 logits，使用 detach 的候选重构进行交叉熵梯度更新。
    - 对数步：冻结权重，使用 stop-gradient 隔离候选重构，仅通过惩罚项（\( \lambda \bar{b} \)）更新 logits（交叉熵不流入 logits）。
    - 交替更新（类似 DARTS），每 m 步更新一次 logits。
  - **固定方案（Recipe）**：通过搜索发现稳定的分配模式后冻结为规则：
    - 第一个 MLP 块：4 位（最敏感）。
    - 其他注意力和 MLP 投影：2 位。
    - 语言模型头：按词频分档（前 0.5% 词为 8 位，1.5% 为 4 位，10% 为 2 位，88% 为 1 位，平均 1.18 位）。
  - **推理内核**：定制融合解量化-乘法 GEMV 内核，直接加载打包的低位权重，避免扩展为 FP16。

### 3. 实验设计
- **数据集**：
  - 发现与消融：TinyStories（2 亿 tokens，GPT-2 tokenizer）。
  - 规模实验：FineWeb-Edu（20 亿 tokens，Qwen tokenizer）。
- **基准（Benchmark）**：验证集困惑度（PPL）、KL 散度、argmax 翻转率。
- **对比方法**：
  - FP16 全精度基线（相同参数量或等存储）。
  - 自适应 VBQ 搜索 vs 固定方案。
  - 均匀 2 位、混合头部（词频分级）等消融。
- **模型规模**：55M、73M、131M、1.46B 参数。额外测试：1.0B Qwen3.5 混合架构（DeltaNet）、9B 混合架构（仅推理速度）。

### 4. 资源与算力
- **未明确说明具体硬件型号和数量**：仅提及使用 AdamW 优化器，TinyStories 运行 35K 迭代，FineWeb-Edu 运行约 1.4 tokens/参数。9B 训练因计算预算不足而未完成。
- **估算**：所有实验在一个研究环境下完成，但无 GPU 型号、数量、总时长等具体数据。

### 5. 实验数量与充分性
- **实验数量**：较多，覆盖多个规模、两个数据集、两种架构（标准 transformer、Qwen3.5 混合架构）、完整消融（均匀 2-bit、混合头部、1.3×/2.4× 放大、深度 vs 宽度、冻结后校正）、分布分析（KL、flip）、推理速度基准。
- **充分性与公平性**：严格控制同架构 FP16 基线、等参数量或等存储对比；消融实验系统，证明了固定方案可复用。但缺少更大规模（>1.5B）的完整质量验证和下游任务评测（MMLU、GSM8K 等），作者已诚实指出。

### 6. 主要结论与发现
- **强非均匀分配**：搜索发现 69.3% 组选择 1 位，20.5% 选择 2 位，仅 10% 左右选择 4/8 位；语言模型头平均仅 1.09 位。
- **深度自愈量化误差**：早期 4 位块引入最大 KL 散度（0.83 nats，71% flip），后续 2 位块逐步修正，最终 flip 率降至 28%（经过共适应头后为 18.7%）。
- **更大但更小**：
  - 131M 模型平均 1.82 位，PPL 4.2，超越 55M FP16（PPL 4.4），存储减少 3.8 倍。
  - 1.46B 模型匹配 593M FP16，存储减少 3.7 倍，参数多 2.5 倍。
  - 质量/字节效率比 FP16 高 3.9–8.4 倍。
- **推理加速**：在 Apple M4 Max 上，1B 模型解码快 1.86 倍，9B 模型快 4.68 倍（同输出精度），加速比随模型规模增长。

### 7. 优点
- **方法创新**：首次在 LLM 中实现每组精度的可学习分配，通过梯度隔离和交替优化克服了训练困难。
- **可迁移性**：发现的分配模式冻结为固定方案后，可直接用于新架构（如 DeltaNet），无需重新搜索。
- **实际加速**：打包存储直接映射到自定义内核，实现真实推理加速，且加速比随规模增大。
- **深入分析**：通过 KL 散度和 flip 率揭示深度自愈机制，解释为何低位宽仍能奏效。

### 8. 不足与局限
- **适用范围**：仅适用于从头训练，无法作为后训练压缩工具（PTQ 场景下不如 GPTQ/AWQ）。
- **扩展性**：>1.5B 参数的经济训练仍未验证（9B 训练因预算不足而中断，且存在覆盖率 bug）。
- **缺乏下游任务评测**：仅报告 PPL 和分布距离，未提供 MMLU、GSM8K 等标准基准分数。
- **收敛慢**：VBQ 模型需要更多训练步骤才能达到同等质量（约 46% 训练后超越 FP16 基线）。
- **深度不是宽度替代**：单纯增加深度收益递减，更宽的架构仍必要。

（完）
